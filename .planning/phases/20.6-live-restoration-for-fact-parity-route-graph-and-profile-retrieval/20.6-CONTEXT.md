# Phase 20.6 Context

## Why This Phase Exists

Phase `20.5` closed the proof-calibration question, but it did not restore the live benchmark-path kernel.

Current live truth after `20.5`:

- answer-only: `4 / 15`
- split raw answer score: `6 / 15`
- split `retrieval_correct`: `5 / 15`
- split `both_correct`: `4 / 15`
- route activation still effectively dead:
  - `route_source=default`: `14 / 15`
  - `applied_mode=fact`: `14 / 15`
  - `applied_mode=aggregate`: `1 / 15`
- graph yield still sparse:
  - `Chroma` nonzero: `15 / 15`
  - `SQLite` graph nonzero: `3 / 15`
  - `Kuzu` graph nonzero: `3 / 15`

This means the next phase is no longer about proving the metric. It is about restoring the live benchmark-path retrieval stack itself.

## What This Phase Is

This is a live restoration phase with four bounded restoration targets:

1. fact-path parity restoration against the earlier stronger decomp-off path
2. route activation and graph yield recovery as one interdependent restoration problem
3. explicit audit of profile / identity retrieval misses such as `5d3d2817`
4. bounded audit of retrieval-correct but answer-wrong grounding leaks such as `d682f1a2`

## What This Phase Is Not

This phase is **not**:

- a new proof-calibration phase
- a new decomposition phase
- a bonus / wording retuning phase
- a donor rewrite phase
- a benchmark-gaming phase

## Entry Conditions

Phase `20.5` completed with these explicit unresolved gaps:

- live fact-path parity remains far below the old `20.2` decomp-off path
- structural routing almost never activates in live runs
- graph yield remains sparse before `Kuzu`-specific blame is justified
- at least one clean retrieval-correct / answer-wrong case exists
- at least one explicit profile / identity retrieval miss exists

## Target End State

At the end of `20.6`, the system must be able to say one of two things clearly:

- live restoration materially improved and the route / graph / fact-path gaps measurably narrowed

or

- live restoration remained insufficient, with the blocking layer named precisely

The phase must not hide behind better measurement alone.
