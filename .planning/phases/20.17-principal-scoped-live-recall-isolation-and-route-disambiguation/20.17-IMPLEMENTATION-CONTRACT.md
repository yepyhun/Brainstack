# Phase 20.17 Implementation Contract

## Objective

Fix the two strongest live blockers carried out of `20.16`:

1. principal-scoped recall leakage
2. ambiguous noun-`order` route misclassification

Then verify the result against the corrected isolated live reset baseline.

## Workstream A: Principal-Scoped Recall Isolation

- Start from the `20.16` packet-localization artifact.
- Prove where unrelated session/user transcript rows enter the packet.
- The phase must explicitly classify the seam:
  - transcript search scope
  - continuity search scope
  - packet merge scope
  - or another bounded path
- The fix must preserve legitimate same-principal long-range recall.

## Workstream B: Route Disambiguation

- Start from the `coffee_order` isolated live fail.
- Fix the deterministic route policy so noun uses of `order` do not automatically imply temporal sequencing.
- The fix must remain generic:
  - no scenario names
  - no benchmark-specific word bundles
  - no local one-off guard just for cafe orders

## Workstream C: Corrected Live Proof

- Re-run the faithful isolated reset eval after A/B land.
- Compare against:
  - `20.16` isolated reset baseline: `3 / 5`
- Record:
  - pass/fail by scenario
  - whether unrelated memories still surface
  - whether route choice changed on ambiguous `order` prompts

## Workstream D: Honest Next-Phase Decision

- If `20.17` materially improves the live reset baseline, explicitly choose the next thread.
- If profile-style durable recall is now the dominant blocker, say so directly.
- If reset-boundary bridge weakness remains, say so directly.
- State explicitly whether the deployed Bestie runtime is still running with `semantic` / `graph` channels degraded, so graph/corpus-backed live enablement remains a separate visible roadmap item.
- Do not reopen broader aggregate expansion unless the live recall integrity blockers are no longer dominant.

## Protected Boundaries

### Product Boundary

- faithful live eval beats oracle intuition
- coherent conversation and correct continuity outrank benchmark-shaped broadening

### Architectural Boundary

- no donor-breaking local hacks
- no half-wired cross-store shortcuts
- no broad profile-system rewrite hidden inside a routing fix

### Anti-Benchmaxing Boundary

- no case-specific lexical hacks
- no route rules tailored to named benchmark IDs
- no fake gains from harness manipulation

## Minimum Evidence Required Before Calling Phase 20.17 Done

- one explicit leakage-source diagnosis
- one explicit route-disambiguation diagnosis
- one corrected isolated live reset-eval artifact
- one before/after read against the `20.16` `3 / 5` baseline
- one explicit next-phase recommendation
