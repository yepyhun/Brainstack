# Phase 20.17 Context

## Why This Phase Exists

- `20.16` closed at gate as a live residual triage phase.
- The corrected isolated reset eval replaced a confounded `0 / 5` with a faithful `3 / 5` baseline.
- The remaining failures are no longer best explained as simple no-write admission misses.
- The next highest-value thread is now live recall integrity and route correctness, not broader aggregate expansion.

## Current Truth Entering 20.17

- `20.15` proved one bounded native aggregate path can exist.
- `20.16` proved the remaining live failures are more specific:
  - the failing facts are persisted into transcript/continuity shelves
  - the live recall path can still surface transcript rows from unrelated isolated users/sessions
  - noun-`order` queries can still misroute into temporal mode
  - the reset/live path still ends with `profile_items = 0`, leaving preference/routine-style recall too dependent on continuity/transcript competition
- The corrected isolated reset eval artifact is:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-bestie-live-reset-eval-isolated.json`
  - result: `3 / 5`
- The localized packet audit artifact is:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-20.16-isolated-live-packet-localization.json`
- That localized live audit also showed the deployed Bestie runtime currently has:
  - `semantic: degraded`
  - `graph: degraded`
- Therefore `20.17` should be evaluated as a live fix on the currently active `keyword + temporal` stack. Graph/corpus-backed live enablement is still a separate explicit thread.

## What This Phase Is

- a principal-scoped live recall integrity phase
- a route disambiguation phase for ambiguous noun-`order` queries
- a bounded reset-boundary bridge hardening phase after isolation and routing are corrected
- a product-quality phase aligned to:
  - coherent continuous conversation
  - proactive stateful continuity
  - long-range accurate recall

## What This Phase Is Not

- not broader aggregate expansion
- not another oracle-heavy benchmark phase
- not temporal micro-tuning revival
- not a broad profile-system redesign
- not a generic reranker/scorer bakeoff
- not a donor-breaking local hack phase

## Entry Conditions

- `20.16` closeout remains the source of truth
- the fixed temporal canary must stay green
- the bounded `20.15` aggregate win must not regress
- any live eval used here must remain faithful:
  - isolated scenario boundaries
  - no `Platform.LOCAL` leakage
  - no harness-induced cross-scenario contamination

## Target End State

- live reset-boundary recall no longer surfaces unrelated other-session / other-user memories
- ambiguous noun-`order` requests no longer fall into temporal mode unless genuine sequencing cues exist
- the corrected isolated live reset eval improves beyond the `20.16` `3 / 5` baseline
- the phase can explicitly say whether any remaining reset-boundary failures are now:
  - principal isolation
  - route choice
  - profile-shelf weakness
  - or a bounded combination
