# Phase 20.18 Plan

## Goal

Separate live low-overlap surfacing failures from answer-synthesis failures on the deployed degraded stack, then land the smallest donor-first fix that improves the structurally under-surfaced class without reopening solved `20.17` seams.

## Scope

### In

- same-principal low-overlap live surfacing on the active degraded stack
- packet-sufficiency vs answer-synthesis separation for the remaining isolated live failures
- one bounded live proof after the fix
- one optional control rerun only if needed to resolve regression-vs-variance ambiguity

### Out

- broader aggregate trigger/query expansion
- graph/corpus runtime enablement
- new principal-scope architecture work unless new evidence proves regression
- benchmark-shaped lexical tuning
- broad prompt or profile-system redesign

## Execution Steps

1. Workstream A: residual-class proof.
   - Start from the latest `20.17` isolated live eval plus packet-localization artifact.
   - Classify each remaining failing scenario as:
     - low-overlap surfacing / coverage failure
     - packet-sufficient but answer-weak failure
     - or mixed
   - Keep the current explicit reading:
     - `temporal_order` is low-overlap surfacing until disproven
     - `gift_context` is synthesis weakness until disproven
     - `coffee_order` is mixed, with a real low-overlap modifier-loss component

2. Workstream B: bounded low-overlap live surfacing fix.
   - Target only the structurally under-surfaced class.
   - Prefer the smallest donor-first change on the active degraded stack:
     - same-principal session-summary carry-through
     - low-overlap same-principal evidence promotion
     - or another equally bounded retrieval/selection tweak
   - Do not reopen principal filtering or route policy unless new evidence requires it.

3. Workstream C: optional control check.
   - If the latest `20.17` score movement still looks ambiguous, run one bounded control comparison to distinguish:
     - patch-caused regression
     - versus model/provider variance
   - This is optional and only justified if the packet evidence alone cannot settle the reading.

4. Workstream D: faithful live proof.
   - Re-run the isolated deployed-live reset eval.
   - Compare against the latest `20.17` repeated baseline:
     - `2 / 5`
   - Record per-scenario packet class and final answer class separately.

5. Workstream E: honest next-phase decision.
   - If low-overlap surfacing materially improves, say so directly.
   - If synthesis weakness remains dominant after packet sufficiency is proven, say so directly.
   - If graph/corpus live enablement now looks like the highest-value next thread, say so directly.

## Implementation Rules

- do not widen retrieval globally just to stuff more rows into every packet
- do not use scenario words like `dentist`, `brake`, `vanilla`, `climbing`, or `scented` as route or surfacing cues
- do not reopen solved `20.17` seams without fresh proof
- keep the live degraded-stack truth explicit:
  - current fixes are acting on `keyword + temporal`, not on semantic/graph channels

## Output Shape Required From Phase 20.18

- one explicit residual-class split for the remaining live failures
- one bounded low-overlap surfacing fix or one honest proof that no such fix is justified yet
- one repeated isolated live eval artifact after the phase change
- one explicit recommendation for the next thread

## Exit Gate

- the phase can explicitly separate:
  - structurally under-surfaced failures
  - from structurally sufficient but answer-weak failures
- the bounded low-overlap surfacing class improves without regressing principal isolation or noun-`order` routing
- the next-thread recommendation is evidence-led, not planning momentum

## Recommended Effort

- `xhigh`
