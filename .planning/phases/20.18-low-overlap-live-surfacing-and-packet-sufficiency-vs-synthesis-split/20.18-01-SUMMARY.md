## Outcome

Phase `20.18` executed the bounded low-overlap live surfacing pass on the deployed Bestie runtime after the `20.17` isolation / route fixes.

This phase did **not** reopen broader aggregate capability work.
It stayed on the active degraded live stack and targeted two bounded questions only:

- can same-principal low-overlap support rows survive into the live packet instead of being dropped by transcript hygiene gates?
- can the remaining live failures now be split honestly into packet-sufficiency vs answer-synthesis vs runtime-enablement classes?

## Source Changes

### Brainstack source

- [executive_retrieval.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/executive_retrieval.py)
  - added bounded same-principal `session_summary -> transcript` support carry-through
  - kept that carry-through alive for temporal mode when the packet already carries structurally meaningful temporal anchors
- [transcript.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/transcript.py)
  - allowed bounded `transcript.session_support` rows to survive transcript hygiene
  - added a minimal plural-query singular fallback in retrieval tokenization for low-overlap lexical misses such as `trips -> trip`
- [db.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/db.py)
  - made principal-scope filtering backward-compatible for legacy rows that do not yet carry explicit principal metadata
- [test_brainstack_phase20_proof.py](/home/lauratom/Asztal/ai/atado/Brainstack/tests/test_brainstack_phase20_proof.py)
  - added `20.18` proof coverage for fact-side same-principal support carry-through
  - added `20.18` proof coverage for temporal same-principal support carry-through

### Bestie runtime mirror

- [executive_retrieval.py](/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-bestie-latest/plugins/memory/brainstack/executive_retrieval.py)
- [transcript.py](/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-bestie-latest/plugins/memory/brainstack/transcript.py)
- [db.py](/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-bestie-latest/plugins/memory/brainstack/db.py)

The runtime mirror was synced directly from the Brainstack source before the live eval rerun.

## Validation

### Targeted source proofs

- targeted phase20 proof slice:
  - `7 passed`
  - included:
    - new `20.18` fact support proof
    - new `20.18` temporal support proof
    - `20.17` principal-scope transcript filter proof
    - `20.17` principal-scope temporal continuity filter proof
    - noun-`order` fact-route proof
    - legacy low-overlap temporal transcript supply proof
    - unicode-safe tokenization proof

### Broader source slice

- [test_brainstack_real_world_flows.py](/home/lauratom/Asztal/ai/atado/Brainstack/tests/test_brainstack_real_world_flows.py)
  - `32 passed`
  - `2 failed`
  - both failures remained tied to degraded graph/corpus runtime (`kuzu` / `chromadb` unavailable), not to the new `20.18` low-overlap carry-through path

### Live eval

- isolated deployed Bestie reset eval:
  - [brainstack-bestie-live-reset-eval-isolated.json](/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-bestie-live-reset-eval-isolated.json)
  - result: `3 / 5`
  - accuracy: `0.6`

Scenario result after the `20.18` patch:

- `dietary_constraint` — pass
- `coffee_order` — pass
- `gift_context` — pass
- `temporal_order` — fail
- `aggregate_total` — fail

## Result

Phase `20.18` is execution-complete at gate as a **bounded live-surfacing improvement**, but **not** as a clean full product pass.

What is now clearly improved:

- the bounded same-principal support carry-through is real in source
- the degraded live stack recovered two previously unstable fact-style scenarios:
  - `coffee_order` now keeps the low-overlap modifiers
  - `gift_context` now answers from the already-sufficient packet instead of collapsing into “I can’t access that”
- the `20.17` principal-scope isolation and noun-`order` route fixes remain intact
- the legacy cross-session low-overlap temporal transcript proof was restored after making the principal filter backward-compatible

What is **not** yet solved:

- `temporal_order` still fails on deployed Bestie
  - the answer is now an “I don’t see the context” style miss
  - this means the degraded live stack is still not surfacing enough same-principal ordering support for that scenario
- `aggregate_total` still fails on deployed Bestie
  - the answer stays `8,900 HUF` instead of `9,900 HUF`
  - the live path still does not surface the native aggregate win from `20.15`

## What This Phase Does Not Claim

- it does **not** claim that broader aggregate recovery is solved
- it does **not** claim that the live Bestie runtime is using the `20.11–20.15` graph/corpus capability stack end-to-end
- it does **not** claim that the isolated live eval is fully green
- it does **not** reopen benchmark-shaped micro-tuning

## Explicit Pending Gaps

- deployed Bestie still runs with `semantic: degraded` and `graph: degraded`
  - the current live truth is still mostly the `keyword + temporal` stack
- `temporal_order` remains the live low-overlap temporal residual on that degraded stack
- `aggregate_total` remains a live bridge failure because the native aggregate path is not active in runtime

## Next Thread Decision

Phase `20.18` says the next honest question is no longer:

- “can we squeeze one more answer out of the degraded keyword stack?”

The next honest question is:

- “when do we enable the already-built graph/corpus capability in deployed Bestie and re-baseline live truth on that real stack?”

Therefore the recommended next step is:

- add and plan `20.19` around **live graph/corpus backend enablement and post-enablement isolated live re-baseline**
- do **not** continue blind keyword/temporal micro-tuning first
