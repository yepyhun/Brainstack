# Phase 20.18 Context

## Why This Phase Exists

- `20.17` closed a real live integrity thread:
  - principal-scoped recall leakage was fixed
  - noun-`order` temporal misrouting was fixed
  - reset-boundary principal carry-through was fixed
- But `20.17` did **not** end in a clean live gate pass.
- The repeated isolated live reset eval stayed at `2 / 5`, so the next step cannot be blind aggregate broadening.

## Current Truth Entering 20.18

- Current live runtime is still:
  - `semantic: degraded`
  - `graph: degraded`
- Therefore the live path under test is still the active degraded `keyword + temporal` stack, not the fuller graph/corpus-backed source capability.
- The `20.17` packet localization artifact is:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-20.17-isolated-live-packet-localization.json`
- The repeated isolated live reset eval artifact is:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-bestie-live-reset-eval-isolated.json`

The strongest current reading is split across two residual classes:

1. low-overlap live surfacing / coverage weakness
   - `temporal_order` still under-surfaces a full same-principal event chain
   - `coffee_order` also still loses low-overlap modifiers like `extra hot` / `no vanilla`, even though route selection is now correct

2. packet-sufficient but weak final answer use
   - `gift_context` already surfaces enough same-principal evidence, including profile-style rows
   - the model still answers as if the details are unavailable

## What This Phase Is

- a low-overlap live surfacing phase on the currently active degraded stack
- a packet-sufficiency vs answer-synthesis separation phase
- a product-truth phase that prefers faithful deployed-live evidence over benchmark-shaped momentum

## What This Phase Is Not

- not broader aggregate trigger expansion
- not graph/corpus live enablement
- not renewed principal-isolation work unless new evidence proves regression
- not scorer/reranker planning
- not another oracle-heavy benchmark phase

## Entry Conditions

- `20.17` closeout is the current live truth
- the phase must preserve:
  - principal-scoped live isolation
  - noun-`order` fact routing
- any new live eval must remain faithful:
  - isolated scenario boundaries
  - deployed Bestie path
  - no `Platform.LOCAL` leakage

## Target End State

- the next packet/localization pass can say which failures are:
  - structurally under-surfaced
  - structurally sufficient but answer-weak
- low-overlap same-principal recall is materially improved on the degraded live stack
- the next phase recommendation is evidence-led:
  - live surfacing follow-up
  - answer-synthesis follow-up
  - or graph/corpus enablement
