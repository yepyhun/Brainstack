# Phase 20.16 Plan

## Goal

Re-triage the post-deploy live residual split after `20.15`, identify whether the dominant next seam is live bridge, live admission residual, or honest broader aggregate expansion, and only then set the right technical follow-up without reopening temporal rescue or lexical counting.

## Scope

### In

- baseline reading of the fixed post-deploy live `12` answer-only slice and the bounded oracle proofs already produced
- one bounded correction of the reset-boundary live eval harness so the phase starts from faithful live evidence rather than a confounded `0 / 5`
- explicit separation of:
  - zero-backend / `empty_text` live failures
  - nonzero-backend wrong-answer live failures
  - transcript-only passes that hide missing structured bridge
- one bounded localization step that proves where the `20.15` native-aggregate oracle win drops out of the default live mixed path
- one bounded localization step on the strongest nonzero-backend wrong-answer case
- fixed canary regression protection after the change

### Out

- temporal micro-tuning as a main thread
- broad aggregate expansion before live residual triage
- Jina v5 / scorer / reranker reopening
- lexical counting heuristics
- full graph schema redesign
- wide answer-only suite runs as inner-loop tools

## Execution Steps

1. Workstream A: baseline the live residual split.
   - Start from the fixed post-deploy `12` live answer-only artifact plus the known bounded oracle proofs.
   - Before treating the deployed reset-boundary `0 / 5` as product truth, correct the harness so that:
     - each scenario is isolated from the prior scenario recall session
     - non-faithful `Platform.LOCAL` tool leakage is avoided or explicitly disabled
     - the phase does not blame `flush_min_turns` unless fresh evidence shows the reset-finalize path is actually gated there
   - Persist the pre-change truth for:
     - whether the case is zero-backend, transcript-only pass, nonzero-backend wrong answer, or bounded structured success
     - whether the relevant oracle-only capability exists for the same class
     - whether the live blocker is admission, surfacing/selection, synthesis, or bridge absence
   - Keep the short-session oracle admission note visible, but do not let it redefine the whole phase.
   - If the corrected reset-boundary eval materially changes the blocker reading, record that before any code change.

2. Workstream B: localize the strongest live bridge miss.
   - Start from the bounded `20.15` aggregate win and prove where that `native_aggregate` row fails to enter the default live mixed path.
   - The phase must explicitly say whether the missing bridge is:
     - planner non-trigger in live mode
     - retrieval suppression / non-surfacing
     - mixed-path row competition
     - synthesis ignoring surfaced native evidence
   - No broader aggregate planner broadening may begin before this localization exists.

3. Workstream C: localize the strongest nonzero-backend wrong-answer class.
   - Use one or more representative live failures where structure exists but the answer is still wrong.
   - Preferred anchors include:
     - `gpt4_7f6b06db` for temporal structure existence without correct final answer
     - one nonzero-backend knowledge-update / aggregate miss
   - The output must separate:
     - structure exists but wrong evidence is selected
     - correct evidence is present but synthesis still fails
     - or another bounded explanation

4. Workstream D: decide the true next technical phase.
   - Based on A/B/C, make one explicit decision:
     - broader aggregate coverage is still next
     - live bridge / mixed-path integration is next
     - admission / `empty_text` reliability is next
     - or a bounded combination with named primary and secondary threads
   - Re-run the fixed retrieval-only canary afterward.
   - Only attach a new bounded proof if the phase actually lands code that changes one residual class instead of just reclassifying it.

## Implementation Rules

- Do not reopen temporal-chain work unless a fresh generic temporal regression appears.
- Do not relabel the oracle short-session residual as the whole product blocker.
- Do not treat the current reset-boundary `0 / 5` as clean product truth without first correcting the harness confounds.
- Do not pretend the current live `6 / 12` split is solved by broader aggregate coverage alone.
- Prefer explicit residual-class localization over speculative capability broadening.
- Keep the intervention bounded enough that the dominant live blocker can still be named.
- If the phase concludes that broader aggregate coverage should wait, say so explicitly.
- Preserve the current temporal-event extraction behavior unless explicit evidence shows a direct conflict with the live residual being investigated.

## Output Shape Required From Phase 20.16

- one explicit statement of the dominant current live blocker:
  - zero-backend / `empty_text`
  - nonzero-backend wrong answer
  - missing oracle-to-live bridge for a bounded capability win
  - or a bounded combination
- one explicit statement of whether `20.15` native aggregate is present in the default live mixed path or still oracle-only
- one explicit statement of whether the next highest-value follow-up is bridge, admission, or broader aggregate coverage
- one explicit statement of whether reset-boundary failure survives a faithful isolated live eval
- one canary retrieval-only regression artifact
- one explicit note on whether the short-session oracle admission edge case changed at all during the phase

## Exit Gate

- one comparable residual-class baseline exists
- one honest decision exists on what the next implementation phase should be
- if code lands, the changed residual class is named explicitly
- canary retrieval-only regression check exists
- the phase can state the next seam without bluffing or benchmark-specific shortcuts

## Recommended Effort

- `xhigh`, because this is the first post-deploy reprioritization step after the bounded `20.15` oracle win
