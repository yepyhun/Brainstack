# Phase 20.16 Implementation Contract

## Objective

Use the post-deploy live evidence to determine whether the true next seam after `20.15` is live bridge, live admission residual, or honest broader aggregate expansion, without reopening temporal rescue, misclassifying oracle artifacts as the whole product diagnosis, or treating a confounded reset-eval as final product truth.

## Workstream A: Live Residual Baseline

- Start from the fixed post-deploy `12` live answer-only artifact plus the proven bounded `20.15` oracle class.
- Correct the deployed reset-boundary eval before relying on it:
  - isolate each scenario from the prior scenario recall session
  - avoid or explicitly disable non-faithful `Platform.LOCAL` tool leakage
  - do not elevate `flush_min_turns` into the main blocker unless the reset-finalize path itself proves that gate
- Persist:
  - whether each named case is:
    - zero-backend
    - transcript-only pass
    - nonzero-backend wrong answer
    - or bounded structured success
  - current typed entity / state / relation population when present
  - whether a bounded oracle capability exists for the same class
  - the dominant blocker category:
    - live admission / `empty_text`
    - missing oracle-to-live bridge
    - nonzero-backend selection / synthesis miss
    - or a bounded combination
- No broader aggregate expansion work may begin before this baseline exists.
- The short-session oracle admission note may be mentioned, but it must not be promoted into a total product diagnosis.
- If the corrected reset-boundary eval materially changes the blocker reading, that new reading becomes the baseline for the rest of the phase.

## Workstream B: Oracle-to-Live Bridge Localization

- Start from the bounded `20.15` native aggregate proof and prove where that win drops out of the default live mixed path.
- The phase must explicitly name whether the bridge blocker is:
  - planner non-trigger
  - row not surfaced
  - row surfaced but lost to mixed-path competition
  - row surfaced but ignored in synthesis
  - or another bounded cause
- This workstream must not silently drift into:
  - benchmark event hardcoding
  - lexical counting heuristics
  - answer-template hacks

## Workstream C: Nonzero-Backend Wrong-Answer Localization

- Use one or more live failures with nonzero backend support.
- The phase must explicitly distinguish:
  - correct structure missing from selection
  - correct structure selected but not trusted
  - synthesis still wrong despite adequate recalled evidence
  - or another bounded cause
- `gpt4_7f6b06db` remains the preferred temporal anchor for this workstream because live `temporal_events` already exist there.

## Workstream D: Honest Next-Phase Decision

- Based on A/B/C, choose one honest next thread:
  - broader aggregate coverage
  - live bridge / mixed-path integration
  - live admission / `empty_text` reliability
  - or a bounded primary/secondary split
- Re-run the fixed retrieval-only canary afterward.
- Only escalate to a broader spillover check if new code lands and actually changes a residual class instead of merely reclassifying it.

## Protected Boundaries

### Anti-Benchmaxing Boundary

- No benchmark-specific entity-name rules.
- No one-off counting regexes.
- No prompt-side arithmetic over packed evidence as a fake aggregate path.
- No reopening of already rejected temporal or lexical micro-tuning loops.
- No treating a `Platform.LOCAL` harness artifact as deployed product truth.

### Architectural Boundary

- Do not reopen whether temporal semantic reranking exists; `20.12` already answered that.
- Do not reopen whether upstream temporal-event generation can exist; `20.13` already answered that under bounded proof.
- Do not reopen whether temporal chain coverage can be preserved; `20.14` already answered that under bounded proof.
- Do not reopen whether one bounded native aggregate path can exist; `20.15` already answered that.
- Keep `session_boundary` as a bounded benchmark-path proof tool only.
- Do not reframe `flush_min_turns` as the main Brainstack reset blocker without direct evidence on the reset-finalize path.

### Scope Boundary

- The phase is about deciding the true next seam after the `20.15` oracle win and the post-deploy live `6 / 12` result.
- The phase must explain whether the dominant blocker is:
  - live bridge
  - live admission residual
  - broader aggregate coverage still waiting behind those
  - or a bounded combination
- The oracle short-session admission note may remain in the carry-forward section, but it must not be reframed as the whole blocker.
- If the phase concludes that broader aggregate should wait, that must be recorded explicitly, not hidden as planning drift.

## Minimum Evidence Required Before Calling Phase 20.16 Done

- one live residual baseline artifact
- one explicit oracle-to-live bridge read on the bounded `20.15` capability
- one canary retrieval-only regression artifact
- explicit statement of the dominant live blocker
- explicit statement whether broader aggregate expansion remains next or should wait behind bridge/admission work
- explicit note whether the short-session oracle admission edge case changed at all
- explicit note whether reset-boundary failure survived a corrected isolated live eval
