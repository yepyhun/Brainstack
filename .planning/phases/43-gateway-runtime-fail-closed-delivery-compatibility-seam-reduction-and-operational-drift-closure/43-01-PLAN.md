# Phase 43 Plan

## goal

Reduce the remaining gateway/runtime best-effort and compatibility-heavy seams so delivery, boot, restart, auth/runtime warnings, and provider fallback behavior are explicit, bounded, and closer to fail-closed inspection standards.

## scope

### in

- `gateway/stream_consumer.py`
- `gateway/run.py`
- runtime warning/auth/provider resolution surfaces implicated by Phase 41
- deploy/runtime drift visibility where source changes can actually help

### out

- giant gateway orchestration decomposition beyond what is necessary to clean fallback seams
- broad session storage migration
- graph value expansion

## canonical principle reference

- `/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-latest/.planning/IMMUTABLE-PRINCIPLES.md`

## workstreams

### 1. delivery fallback reduction

- inspect and reduce best-effort fallback-send behavior in `stream_consumer.py`
- keep user-visible fallback explicit and bounded

### 2. boot/runtime compatibility cleanup

- reduce deprecated env fallback reliance in `gateway/run.py`
- narrow restart and fallback-model semantics to cleaner, inspectable contracts

### 3. runtime warning and provider-failure classification

- separate source defects from deploy/economic drift
- make route/payment/auth warnings operationally legible

### 4. fail-closed uplift where practical

- eliminate silent degrade-open where a truthful explicit failure is feasible

### 5. proof and live verification

- targeted tests and live/deployed parity checks for the corrected runtime seams

## deliverables

- reduced runtime fallback complexity
- clearer provider/runtime failure classification
- tests for the cleaned runtime paths
- verification notes mapped back to Phase 41 Batch 1 and Batch 4 findings

## validation ladder

1. source tests for stream consumer and gateway runtime seam changes
2. installed checkout parity
3. live runtime smoke on restart, delivery fallback, and warning paths
4. explicit classification of what remains deploy-only drift

## recommended model level

- `xhigh`
