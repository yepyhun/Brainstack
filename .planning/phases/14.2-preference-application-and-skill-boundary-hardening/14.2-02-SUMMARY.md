# Phase 14.2 Follow-Up Summary

Date: 2026-04-11

## Why This Follow-Up Exists
- The earlier `14.2` execute improved ownership boundaries and reset behavior, but later live Discord testing exposed a remaining bug cluster:
  - Brainstack stored communication rules, but did not project them back strongly enough
  - recalled memory was wrapped too weakly, so the model could narrate internal memory scaffolding
  - overlapping style rows were fed back too literally

## What Changed
- Hardened the Brainstack read path in source:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/retrieval.py`
- Added an explicit active communication contract section:
  - projected from current Brainstack style/profile facts
  - imperative, compact, private-by-default
- Hid canonical communication-style rows from the normal profile-match echo path once they are already represented by the active contract
- Hardened the installer-carried host patch so Hermes `agent/memory_manager.py` wraps recalled memory as:
  - private internal context
  - to be applied silently
  - not to be mentioned unless the user explicitly asks about memory/debugging
- Added doctor coverage for the private recall wrapper:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/scripts/brainstack_doctor.py`
- Added regression coverage:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/tests/test_brainstack_retrieval_contract.py`
  - `/home/lauratom/Asztal/ai/atado/Brainstack/tests/test_install_into_hermes.py`

## What We Explicitly Did Not Do
- Did not grow Tier-1 extraction heuristics further
- Did not amputate `skill_manage`
- Did not add a new runtime or sidecar
- Did not treat config personality as the root fix

## Verification
- `py_compile` passed for all modified source and test files
- targeted manual source-side smoke passed:
  - active communication contract present in system prompt
  - canonical emoji/style rows hidden from normal profile echo
  - private recall wrapper patch applied correctly
- installer carried the patch into the live Bestie runtime
- doctor passed after reinstall, including:
  - `PASS private_recall_wrapper`
- live installed files now contain:
  - active communication contract rendering
  - private recall wrapper language
- live Docker rebuild completed successfully
- live runtime status after rebuild:
  - `healthy`
  - `running; connected=discord`

## Outcome
- The next 14.2 verification should focus on behavior, not storage:
  - no internal memory narration in user-visible replies
  - stronger immediate obedience to communication style rules
  - no regression in procedural `skill_manage`
