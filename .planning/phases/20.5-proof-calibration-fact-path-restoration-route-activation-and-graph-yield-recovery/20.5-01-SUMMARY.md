# Phase 20.5 Summary

## Verdict

Phase `20.5` execute is complete.

This phase materially improved proof calibration and split-proof visibility, but it did **not** restore live benchmark-path retrieval strongly enough to claim restoration success.

The calibrated result is now clearer than `20.4`, but the live stack remains insufficient:

- answer-only comparability: `4 / 15`
- split-proof raw answer score: `6 / 15`
- split-proof `retrieval_correct`: `5 / 15`
- split-proof `both_correct`: `4 / 15`

So the phase succeeded at **measuring the system more honestly**, but failed to restore the live benchmark path to an acceptable level.

## What Changed

Source-side runner hardening landed in:

- `/home/lauratom/Asztal/ai/atado/Brainstack/scripts/run_brainstack_longmemeval_subset.py`
- `/home/lauratom/Asztal/ai/atado/Brainstack/tests/test_brainstack_longmemeval_subset.py`
- `/home/lauratom/Asztal/ai/atado/Brainstack/tests/test_brainstack_phase20_proof.py`

Implemented in this phase:

- retrieval-judge calibration for direct evidence cases
  - explicit time-format equivalence handling
  - explicit named-token entity support handling
  - derived/integrated answers still blocked from silent promotion
- direct route-hint payload parsing hardened beyond strict JSON-only success
- benchmark seeding hardened with:
  - in-run backend population logging
  - bounded `Tier2` flush cadence
  - larger benchmark transcript window
  - safe `Tier2` fallback when the extractor returns non-JSON
- live rerun observability hardened with:
  - per-question progress events
  - partial report writes after each completed question
  - redirected/logged execution path for long runs

## Validation

Targeted validation passed:

- `tests/test_brainstack_longmemeval_subset.py`
- `tests/test_brainstack_phase20_proof.py`
- result: `28 passed`

## Live Results

### Answer-only

- report:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-final-boss-20.5-answer-only.json`
- score: `4 / 15`
- suspicious answer-judge passes: `2`

This is **worse** than the fixed-clock `20.4` answer-only comparability result of `5 / 15`.

### Split-proof

- report:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-final-boss-20.5-split.json`
- raw answer score: `6 / 15`
- `retrieval_correct`: `5 / 15`
- `both_correct`: `4 / 15`

This means `20.5` improved split-proof interpretability and recovered some retrieval-judge undercount, but still did not restore the live system enough.

## Proven Findings

### 1. Judge calibration improved the metric

`retrieval_correct` moved:

- `20.4`: `2 / 15`
- `20.5`: `5 / 15`

Known false-negative recovery now clearly includes:

- `6a1eabeb`
- `e9327a54`

Both now score as clean retrieval successes under the calibrated split proof.

### 2. Route activation is still effectively dead

Split-proof route distribution:

- `route_source=default`: `14 / 15`
- `route_source=direct_benchmark_route_hint`: `1 / 15`
- `applied_mode=fact`: `14 / 15`
- `applied_mode=aggregate`: `1 / 15`

So route-hint auth is no longer the blocker, but live route activation coverage is still extremely weak.

### 3. Graph yield is still sparse

Across split-proof rows:

- `Chroma` nonzero: `15 / 15`
- `SQLite` graph nonzero: `3 / 15`
- `Kuzu` graph nonzero: `3 / 15`

This preserves the `20.4` conclusion:

- the main graph problem is sparse graph yield
- not a proven `Kuzu`-only publication failure

### 4. Fact-path parity is still not restored

Even with the runner hardening and fixed clock:

- earlier `20.2` decomp-off best known answer-path baseline: `10 / 15`
- `20.4` answer-only comparability: `5 / 15`
- `20.5` answer-only comparability: `4 / 15`

So the live benchmark-path fact stack is still far below the old best-known decomp-off path.

### 5. Route-specific priority preservation did not unlock the routes in practice

The route-specific priority support exists, but because almost every row still stayed on `fact`, the intended structural route recovery did not materialize in the live benchmark run.

## Important Case Reads

### Clean retrieval successes after calibration

- `6a1eabeb`
  - `passed=true`
  - `retrieval_correct=true`
  - `retrieval_judge=yes_answer_found_time_equivalent`
- `e9327a54`
  - `passed=true`
  - `retrieval_correct=true`
  - `retrieval_judge=yes_answer_supported_by_named_tokens`

### Derived / integrated cases remain outside clean retrieval success

- `9ee3ecd6`
  - answer passed
  - retrieval still not promoted
- `69fee5aa`
  - retrieval not promoted
  - still fails as a knowledge-update integration case

### Explicit LLM-answer failure with calibrated retrieval success

- `d682f1a2`
  - `retrieval_correct=true`
  - `passed=false`
  - `failure_layer=llm_answer`

This proves the split proof is now doing useful layer separation instead of flattening everything into raw answer score.

## Interpretation

Phase `20.5` should be treated as:

- **successful proof calibration**
- **successful long-run runner hardening**
- **failed restoration**

That distinction matters:

- the measurement got better
- the live benchmark-path kernel did not recover enough

This phase therefore must not be written up as a partial restoration win.

## Explicit Remaining Gaps

- live fact-path parity remains unresolved
- route activation coverage remains unresolved
- sparse graph yield remains unresolved
- answer-only score remains too low to support a restoration claim

## Recommended Next Step

Plan a follow-up phase focused on live benchmark-path restoration using the now-calibrated proof:

- fact-path parity gap
- route activation coverage gap
- sparse graph-yield gap

Do **not** reopen answer-score laundering or loose benchmark gaming.
