# Phase 54 Implementation Contract

## invariant

The remaining Discord-surface defect must be fixed by strengthening the native explicit-truth contract, not by reintroducing Brainstack governance, heuristic profile parsing, or reply-time policy hacks.

## canonical principle reference

Use the canonical principles file directly:
- `/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-latest/.planning/IMMUTABLE-PRINCIPLES.md`

Pinned names that must govern this phase:
- `Donor-first`
- `Modularity / Upstream updateability`
- `Fail-closed upstream compatibility`
- `Truth-first / no "good enough"`
- `Zero heuristic sprawl`
- `Multimodal-first architecture`
- `The donor-first elv marad`

## required properties

- explicit user/addressing truth remains host-owned on the native explicit-memory seam
- Brainstack mirrors native explicit truth one-way and does not become a parallel profile authority
- platform handle metadata is not allowed to outrank explicit addressing truth on the user-facing Discord surface
- explicit multi-rule teaching can persist as first-class durable truth without depending on hidden free-chat style mining
- the native explicit-memory seam can persist:
  - one entry
  - multiple independent entries atomically
  - one explicit multi-line pack entry
  without introducing a narrow personal schema
- the write path remains generic:
  - no narrow personal-field schema
  - no locale-specific acceptance logic
  - no text-only kernel assumption
- the fix remains inspectable and bounded to the true owner seams

## prohibited outcomes

- adding new behavior-policy or output-governor logic to make greetings or style “behave”
- adding canonical field enums, validation tables, or hardcoded personal-attribute parsers
- letting transport/platform metadata remain a rival truth owner once explicit addressing truth exists
- solving explicit pack persistence with transcript mining, cue farms, or ordinary-turn communication projection
- solving explicit pack persistence with a second hidden style-governor or pack-specific extractor
- moving explicit truth authority back into Brainstack
- introducing a fix that only works for the current `Tomi` / `LauraTom` example instead of the general defect family

## proof expectation

The phase is not complete unless all of these are proven:

- explicit addressing truth is present and wins over handle metadata on the Discord surface
- explicit multi-rule teaching persists as durable truth in one-message and two-message cases
- the generic atomic write path proves:
  - multi-entry explicit truth survives as distinct durable entries
  - a multi-line explicit pack survives as one durable entry
- Brainstack mirror/no-bounce remains correct
- no user-surface leak is introduced by the fix
- the relevant `53` gates still pass after the change

## output required

- one narrow file-level implementation on the true owner seams
- one explicit proof artifact for addressing precedence
- one explicit proof artifact for multi-rule pack persistence
- one live Discord verification note covering the exact residual defect family
