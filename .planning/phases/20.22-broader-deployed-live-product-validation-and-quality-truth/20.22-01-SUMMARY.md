## Phase 20.22 Summary

### Status

- `20.22` is execution-complete at gate.
- No product code changed in this phase.

### What Ran

- initial broader deployed-live eval matrix on a temp-home harness that accidentally forced `custom + MiniMax-M2.7`
- direct provider control on that same wrong path
- corrected broader deployed-live eval matrix on an isolated temp home built from the actual deployed Bestie `config.yaml + auth.json`
- one truthful chain probe on that same actual deployed provider/config path for packet/token evidence

### Primary Artifacts

- broader deployed-live eval:
  - `reports/phase20/brainstack-20.22-broader-deployed-live-eval.json`
- corrected packet/token truth:
  - `reports/phase20/brainstack-20.21-chain-probe.json`
- initial wrong-path control:
  - `reports/phase20/brainstack-20.22-provider-direct-control.json`

### Outcome

- broader matrix result:
  - `8 / 8`
- category read:
  - coherent continuous conversation: `1 / 1`
  - stateful continuity after reset: `2 / 2`
  - long-range recall / relation-tracking: `4 / 4`
  - larger knowledge-body recall: `1 / 1`
- corrected lightweight packet / token truth from the truthful chain probe:
  - selected rows:
    - `matched = 6`
    - `transcript_rows = 5`
    - `graph_rows = 3`
    - `corpus_rows = 0`
  - selected excerpt chars:
    - `2207`
  - selected excerpt token estimate:
    - `552`
  - graph state remained materially present:
    - `interesting_state_count = 12`
    - aggregate recall answer: `605 miles`

### Critical Correction

- the first `20.22` red result was not truthful product evidence
- the harness had accidentally diverged from deployed reality:
  - it force-wrote a temp config that used `custom + MiniMax-M2.7`
  - the actual deployed Bestie config uses:
    - `provider: nous`
    - `model: xiaomi/mimo-v2-pro`
- the wrong-path direct control returned:
  - `choices = null`
  - `base_resp.status_code = 2056`
  - `base_resp.status_msg = "usage limit exceeded"`
- that artifact was useful only as a harness mismatch diagnosis, not as the final product verdict

### Honest Reading

- `20.21` still stands as a real live runtime win
- after correcting the harness to the actual deployed provider/config path, `20.22` shows the broader deployed-live product read is healthy on the current stack
- the main truth now is not “provider quota blocker”
- the main truth is:
  - the broader deployed-live scenarios used here all passed on the actual deployed path
  - packet burden is nontrivial but still bounded in the measured aggregate probe
  - there is no new dominant memory-kernel blocker surfaced by this phase

### Next Step

- checkpoint current `20.21–20.22` live truth
- if continuing in GSD, prefer a broader deployed-live quality/coverage expansion thread rather than another narrow memory micro-phase
