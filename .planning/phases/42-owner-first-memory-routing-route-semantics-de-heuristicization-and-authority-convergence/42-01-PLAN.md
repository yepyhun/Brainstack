# Phase 42 Plan

## goal

Close the remaining Brainstack routing and authority residuals so memory route selection, exact recall, ordinary-turn guidance, and fallback behavior are driven by owner-derived signals and explicit authority lineage instead of query-shape heuristics or cue-table synthesis.

## scope

### in

- `plugins/memory/brainstack/control_plane.py`
- `plugins/memory/brainstack/executive_retrieval.py`
- `plugins/memory/brainstack/retrieval.py`
- related route/debug/authority lineage storage or rendering helpers as needed
- follow-up proof that:
  - exact recall
  - ordinary-turn lane
  - route semantics
  - fallback semantics
    all converge on the same authority lineage

### out

- gateway runtime decomposition
- legacy storage/config retirement
- broad graph value expansion
- giant gateway/run-agent refactors beyond what is strictly required for routing truth convergence

## canonical principle reference

- `/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-latest/.planning/IMMUTABLE-PRINCIPLES.md`

## workstreams

### 1. remove residual query-shape authority

- demote or eliminate remaining cue-table authority in `control_plane.py`
- ensure query-shape may tune presentation only where still justified, not truth authority

### 2. converge route semantics on owner-derived signals

- reduce dependence on cue families and optional route-hint calls in `executive_retrieval.py`
- make route fallback explicit, deterministic, and lineage-aware

### 3. converge exact recall and ordinary-turn authority

- ensure exact archival contract and ordinary-turn lane share the same source generation / lineage
- prevent pseudo-contract or pseudo-authority reconstruction from fallback shelves when canonical authority exists

### 4. harden fallback truthfulness

- keep fallback explicit
- avoid silent route collapse that changes surfaced truth without proof surface visibility

### 5. add deterministic proof

- replay and unit coverage for:
  - route failure
  - unsupported resolver payloads
  - style-contract recall
  - task/operating structured miss behavior
  - authority lineage parity

## deliverables

- corrected routing/authority code
- deterministic lineage/debug metadata for the memory surfaces touched
- targeted regression tests
- verification artifact showing removal of the audited residuals from Phase 41 Batches 2 and 5 where applicable

## validation ladder

1. targeted source tests for control-plane, executive retrieval, and retrieval lineage
2. replay proving exact recall and ordinary-turn lane point at the same authority generation
3. install parity into deployed checkout
4. live smoke for route failure, style-contract recall, and structured miss visibility

## recommended model level

- `xhigh`
