# Phase 20.1 Summary

## Outcome

Phase `20.1` executed the bounded fidelity-repair pass that follows the mixed Phase `20` verdict.

This phase did **not** reopen the donor architecture.
It repaired the benchmark-exposed evidence path on top of the recovered stack:

- conversation history now participates in semantic retrieval
- retrieved conversational evidence now carries explicit date labels when known
- legacy graph ingress no longer emits the junk `role=...` state family
- transcript evidence is wider and detail-preserving instead of living behind the old one-snippet bottleneck
- prompt guidance now prefers specific, non-conflicted recalled facts over generic prior knowledge without introducing blind memory supremacy

## Source Changes

- updated [db.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/db.py)
  - generalized semantic publication through the existing `Chroma` path
  - added conversation-transcript semantic publication and replay
  - added semantic conversation search
  - added optional imported/event-time support for transcript and continuity writes
- updated [corpus_backend.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/corpus_backend.py)
  - semantic backend contract now supports bounded metadata filtering
- updated [corpus_backend_chroma.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/corpus_backend_chroma.py)
  - semantic search can now filter by semantic class
  - metadata now distinguishes corpus vs conversation semantic objects
- updated [executive_retrieval.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/executive_retrieval.py)
  - semantic channel now fuses conversation-semantic evidence into transcript recall
- updated [retrieval.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/retrieval.py)
  - added date-rich transcript/continuity rendering
  - replaced shallow continuity rendering with packed evidence rows
  - widened transcript snippet preservation
  - moved the narrow evidence-priority rule into an always-rendered working-memory section so it survives empty communication-contract cases
- updated [control_plane.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/control_plane.py)
  - widened transcript policy ceilings for normal and temporal queries
- updated [__init__.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/__init__.py)
  - provider defaults now align with `Chroma` as the default corpus backend target
  - `sync_turn()` now accepts optional imported/event timestamps
- updated [continuity_adapter.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/donors/continuity_adapter.py)
  - continuity/transcript writes can preserve imported event time
- updated [graph.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/graph.py)
  - removed the legacy regex role-ingress branch that produced junk truth rows
- updated [run_brainstack_longmemeval_subset.py](/home/lauratom/Asztal/ai/atado/Brainstack/scripts/run_brainstack_longmemeval_subset.py)
  - benchmark seed config now explicitly enables `Kuzu` + `Chroma`
  - haystack session dates now flow into imported turn timestamps
- updated [install_into_hermes.py](/home/lauratom/Asztal/ai/atado/Brainstack/scripts/install_into_hermes.py)
  - patched memory wrapper now carries the narrow evidence-priority rule

## Validation

### Targeted source regression

- targeted Phase `20.1` source suite:
  - `tests/test_brainstack_corpus_backend_chroma.py`
  - `tests/test_brainstack_transcript_shelf.py`
  - `tests/test_brainstack_graph_ingress.py`
  - `tests/test_install_into_hermes.py`
  - `tests/test_brainstack_phase20_proof.py`
  - `tests/test_brainstack_executive_retrieval.py`
- result:
  - `23 passed`

### Bounded eval ladder

- [run_brainstack_phase20_eval_ladder.py](/home/lauratom/Asztal/ai/atado/Brainstack/scripts/run_brainstack_phase20_eval_ladder.py)
- result:
  - Gate A: passed
  - Gate B: passed
  - Gate C: passed
  - Gate D: passed
  - Gate E: skipped because `COMET_API_KEY` / `COMETAPI_API_KEY` was not present in the process environment

### Change-aware review

- code graph delta review completed against the touched files
- no new affected flows were reported
- remaining graph-review warnings are generic broad-file “test gap” hints, not failures in the new 20.1 seams

## Result

Phase `20.1` materially improves the memory-fidelity path without rolling back the donor-first recovery:

- the semantic leg can now see conversation history, not only corpus documents
- imported conversation history can preserve explicit event dates for temporal rendering
- graph truth is cleaner because the known junk role family is blocked at ingress
- transcript evidence now carries more raw answer-bearing detail
- prompt-side evidence priority is now explicit but still narrow and honest
- the benchmark path now actually renders that evidence-priority rule even when there are no communication preferences

The bounded proof was strong enough to justify Phase `20.1` verify, and the verify pass was completed.

What happened next:

- a fresh final-boss rerun was executed on the real Brainstack path
- the benchmark moved from `3 / 15` to `9 / 15`
- this confirms the donor-first recovery direction is materially working

What is **still not** claimed:

- full restoration verdict
- donor-level final-boss fidelity

The new routing is therefore `20.2`, focused on targeted high-precision conversational fact retrieval recovery rather than another broad architecture pass.

## Follow-up

- route to `20.2` high-precision conversational fact retrieval recovery
- after `20.2`, rerun the final-boss path again
- only then refresh the Phase `20` restoration verdict
