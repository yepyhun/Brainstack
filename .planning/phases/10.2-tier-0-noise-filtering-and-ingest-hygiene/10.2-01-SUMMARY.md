# Phase 10.2 Summary

Status: completed  
Date: 2026-04-11

## What Changed
- Added a Brainstack-owned Tier-0 durable-admission module:
  - `brainstack/stable_memory_guardrails.py`
- Added transcript helper support for quoted role-dump detection:
  - `brainstack/transcript.py`
- Wired durable profile admission through Tier-0 in the live provider path:
  - `brainstack/__init__.py`

## Required Noise Families Now Blocked
- markdown tables
- code fences / code-like blobs
- quoted transcript dumps with `User:` / `Assistant:` line prefixes
- uploaded document-wrapper style text
- structured technical / analysis blobs

## Precision Preserved
- direct first-person self-statements still pass
- raw transcript and continuity capture still happen even when durable admission is denied
- no new language-specific graph regex expansion was introduced

## Local Verification
- targeted tests passed:
  - `test_brainstack_real_world_flows.py`
  - `test_brainstack_integration_invariants.py`
- result: `10 passed`

## Installed Runtime Verification
- Reinstalled updated Brainstack into:
  - `/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-bestie-latest`
- Smoke proof executed against the installed runtime plugin path with a temporary SQLite DB:
  - noise marker did **not** land in `profile_items`
  - direct preference marker **did** land in `profile_items`
  - transcript still retained the noisy turn as raw evidence
- Thread proof:
  - a background-thread `sync_turn(...)` write completed without the old SQLite thread exception

## Important Runtime Note
- The main Bestie `brainstack.db` file is host-readonly because it was created by the running container.
- For this phase, the bounded proof used the installed runtime code with a temporary SQLite DB instead of mutating the live production DB file from the host.

## Outcome
Phase 10.2 succeeded: the profile shelf no longer accepts the obvious pasted junk classes that were poisoning durable memory, while direct user preferences still persist.
