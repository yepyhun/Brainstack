# Phase 20.19 Summary

## Verdict

`20.19` is execution-complete at gate, but not as a full clean live pass.

The phase delivered a real runtime enablement win:

- the Bestie checkout `.venv` now imports `kuzu` and `chromadb`
- the live Brainstack provider can now instantiate `KuzuGraphBackend` and `ChromaCorpusBackend`
- the post-enablement isolated live baseline was rerun on the enabled stack

The phase did **not** prove a fully clean end-to-end bridge from enabled graph/corpus capability to stable final live answers on every run.

## What changed

- Installed bounded runtime dependencies into the deployed Bestie checkout `.venv`:
  - `kuzu==0.11.3`
  - `chromadb==1.5.7`
- Produced activation proof:
  - `reports/phase20/brainstack-20.19-activation-proof.json`
- Produced a fail-closed doctor read:
  - `reports/phase20/brainstack-20.19-doctor.txt`
- Re-ran the isolated deployed-live reset eval on the enabled stack:
  - `reports/phase20/brainstack-bestie-live-reset-eval-isolated.json`
- Ran a harness-faithful live debug slice with candidate debug enabled:
  - `reports/phase20/brainstack-20.19-live-debug.json`

## Evidence

### Activation proof

`brainstack-20.19-activation-proof.json` shows:

- `graph_backend_name = "kuzu"`
- `corpus_backend_name = "chroma"`
- `graph_backend_active = true`
- `corpus_backend_active = true`
- both backing paths were created under the temp `HERMES_HOME`

### Doctor truth

`brainstack-20.19-doctor.txt` now passes the runtime dependency checks:

- `graph_backend_dependency: PASS`
- `corpus_backend_dependency: PASS`

But the checkout-level config truth is still not fully green:

- `config_provider: FAIL` (`memory.provider is None`)
- `native_memory_disabled: FAIL`
- `brainstack_plugin_config: WARN`

So the phase can honestly claim **runtime dependency enablement**, but not a fully clean checkout-config ownership story from this session.

### Post-enablement isolated live baseline

`brainstack-bestie-live-reset-eval-isolated.json` after enablement:

- `3 / 5`
- accuracy: `0.6`
- elapsed: `209.72s`

Scenario reading:

- `dietary_constraint`: pass
- `aggregate_total`: pass
- `coffee_order`: pass
- `temporal_order`: fail
- `gift_context`: fail

This is materially different from the degraded-stack reading because `aggregate_total` now passes with the correct `9,900 HUF`, but it is **not** a decisive overall score lift.

### Harness-faithful live debug

`brainstack-20.19-live-debug.json` shows:

- `semantic` is no longer degraded:
  - `reason = "Semantic retrieval is served by corpus.chroma."`
- `graph` is no longer degraded:
  - `reason = "Graph retrieval is served by graph.kuzu."`

And under this debug slice:

- `aggregate_total`: pass
- `temporal_order`: pass
- `gift_context`: pass

However, the same debug artifact also shows that the final selected rows are still dominated by transcript/continuity:

- `graph_rows = 0`
- `corpus_rows = 0`

So the enabled stack is live, but the final answer path is still not consistently *using* the newly enabled graph/corpus evidence as the decisive surfaced block.

## Honest reading

`20.19` proved three things:

1. The degraded `semantic` / `graph` status was primarily a real runtime dependency problem in the Bestie `.venv`.
2. That runtime dependency problem is now resolved in the eval/runtime path used for isolated live checks.
3. The remaining live problem is no longer “backend unavailable”; it is now an **enabled-stack bridge / stability** problem:
   - the final packet is still often transcript/continuity-dominant
   - native graph aggregate evidence is not yet the stable surfaced winner
   - live answer correctness still shows run-to-run instability on the enabled stack

## Next step

Do **not** reopen degraded-stack keyword/temporal micro-tuning.

The honest next GSD step is:

- add and plan `20.20`

Recommended `20.20` focus:

- enabled-stack graph/corpus -> final packet bridge
- native aggregate row surfacing on the live path
- enabled-stack isolated live stability re-baseline
- keep checkout/deploy config truth explicit, not hand-waved
