# Phase 20.11 Summary

## Status

Execution-complete at gate. Cheap structural donor-first retrieval fixes delivered their bounded wins; the remaining temporal gap is handed off to `20.12` as a capability-phase problem.

## Closure Verdict

- `20.11` produced real generic wins and should be counted as successful on its intended scope:
  - patched-runtime canary harness truth was established and kept fast enough for real iteration
  - `5d3d2817` materially improved after the continuity-to-transcript join fix and now carries the answer-bearing `turn 126` row into the live memory block
  - deterministic route policy was restored on the live path
  - compact Tier-2 output rules, JSON repair, and reasoning-field fallback materially improved temporal admission / write reliability
  - a dedicated cross-session `temporal_event` surfacing path now exists
- `20.11` did **not** close `gpt4_7f6b06db`, but the remaining blocker is now much narrower than at phase start:
  - route activation is no longer the problem
  - the relevant trip window can now be admitted and written
  - temporal events can now be surfaced
  - the remaining miss is better read as temporal-event relevance discrimination once many structured temporal rows compete for the final selected block
- that remaining gap should no longer be chased with more `20.11` micro-tuning:
  - do not reopen route-hint work
  - do not reopen shortlist / query-shaping / search-pool widening
  - do not turn `20.11` into a LongMemEval solver phase
- the correct handoff is now:
  - close `20.11` with its current generic wins
  - move the residual temporal-event semantic retrieval / reranking problem into `20.12`

## Current Canary Artifact

- patched-runtime retrieval harness:
  - [brainstack-final-boss-20.11-canary-retrieval-rerun.json](/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-final-boss-20.11-canary-retrieval-rerun.json)
  - [brainstack-final-boss-20.11-canary-post-join-fix.json](/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-final-boss-20.11-canary-post-join-fix.json)
- runtime sync:
  - `ok: true`
  - `compared_files: 28`
  - `mismatch_count: 0`
- canary:
  - `5 / 5` completed
  - `memory_context_present: 5 / 5`
  - non-fact routes: `2 / 5`
  - elapsed: `374.734s`

## Canary Baseline Reading

- `c8c3f81d`
  - still the cleanest fact baseline
  - fact route via `deterministic_route_hint`
  - keyword evidence present and memory block looks sane
- `5d3d2817`
  - still broken as a named profile / identity retrieval miss
  - fact route stayed correct, but this case is the one canary row still using plain `default` rather than `deterministic_route_hint`
  - channel counts are not low (`semantic: 8`, `keyword: 12`, `temporal: 6`)
  - live memory block still remains irrelevant
  - direct debug earlier proved relevant transcript rows exist in storage
  - therefore the remaining bug is not simple absence of raw evidence; it is deeper live selection / fusion / packing on the fact path
- `e9327a54`
  - fact route stayed correct via `deterministic_route_hint`
  - Sugar Factory now surfaces correctly again in the transcript evidence block
  - this is now a confirmed generic fact-path packing win, not a routing problem
- `gpt4_7f6b06db`
  - temporal route activated correctly
  - requested/applied mode are both `temporal` via `deterministic_route_hint`
  - structural channels are not blank (`temporal: 9`, `graph: 3`)
  - packed evidence still missed the actual Muir Woods / Big Sur / Yosemite trip set
  - current targeted debug suggests this is upstream of final packing:
    - Yosemite-like material appears, but the actual trip set is largely absent from the fused pool
    - this now looks more like temporal candidate generation / extraction weakness than a pure route-policy problem
- `6c49646a`
  - aggregate route activated correctly via `deterministic_route_hint`
  - graph yield was nonzero (`6`)
  - aggregate path is not blank, though evidence quality is still not yet proven here

## Dominant Current Findings

- the mandatory `20.11` baseline exists and runtime parity held
- restored deterministic route policy is now live on the patched runtime
- route activation is no longer the first blocker on the canary
- the latest post-join-fix canary retrieval-only rerun now also shows:
  - `5 / 5` completed
  - elapsed: `318.259s`
  - `memory_context_present: 5 / 5`
  - non-fact routes: `2 / 5`
- `5d3d2817` is no longer the leading canary blocker
  - fact route stayed correct
  - the final live memory block contains the answer-bearing `turn 126` marketing-specialist transcript row
  - the full canary retrieval harness now answers it correctly
  - the class is improved materially, though not yet declared generically solved
- the strongest live blockers now look like:
  - temporal candidate generation / extraction, then structural packing (`gpt4_7f6b06db`)
  - remaining junk suppression / packing quality on fact/profile cases after the answer-bearing row already survives
  - aggregate evidence quality (`6c49646a`) remains secondary and not yet proven
- `20.11` should not run canary answer-only yet
  - the retrieval-only baseline already shows obvious retrieval defects worth fixing first

## Additional `20.11` Live Follow-Up Findings

- a real half-wired live-path bug was surfaced and fixed during `20.11`:
  - `brainstack/db.py` referenced `profile_priority_adjustment(...)` without importing it
  - this caused live `search_profile()`-path crashes in patched-runtime single-question repros
  - the fix is now in source and resynced to Bestie
- a generic transcript-row prioritization patch also landed in `executive_retrieval.py`
  - it prefers stronger lexical evidence and more compact transcript rows before packing
  - source-side regression tests stayed green after relaxing the stale `20.3` expectation that fact mode must never surface all three rows in a small toy ordering fixture
- a source/runtime route-policy drift was also found and corrected during `20.11`:
  - the source had drifted away from the recorded `20.8` replace decision and was still defaulting to the LLM route-hint path
  - the bounded deterministic default route resolver was restored in source and resynced to Bestie
- the post-restore canary retrieval-only rerun changed the live picture in a useful way:
  - runtime sync still held (`28` compared files, `0` mismatch)
  - `5 / 5` retrieval-only canary questions completed again
  - elapsed time is now bounded at `303.93s`, so the new canary loop is still materially faster than the old full-15 inner loop
  - route source distribution is now:
    - `deterministic_route_hint`: `4 / 5`
    - `default`: `1 / 5`
  - non-fact routes were restored to `2 / 5`
    - `gpt4_7f6b06db` now runs `temporal`
    - `6c49646a` now runs `aggregate`
  - `e9327a54` is now a genuine fact-path win
    - `turn 50` with Sugar Factory now leads the transcript evidence block in the patched canary
  - `5d3d2817` improved in a meaningful live-path way
    - a structural continuity-to-transcript carry-through join bug was identified and fixed
      - continuity and transcript rows for the same turn did not share raw `id`, so carry-through had to join by `session_id + turn_number` rather than raw `id`
    - the final packed memory block now includes the actual answer-bearing marketing-specialist transcript row (`turn 126`) alongside the later senior-marketing-analyst row (`turn 128`)
    - the targeted patched-runtime retrieval harness now answers this case correctly
    - therefore this is no longer a pure carry-through miss; the remaining issue is residual junk around a now-present answer-bearing row
  - `gpt4_7f6b06db` is now more clearly constrained:
    - route-policy contamination is gone in the live canary
    - it now runs live as `requested=temporal`, `applied=temporal`, `route_source=deterministic_route_hint`
    - even with nonzero structural counts (`temporal: 10`, `graph: 4`), the packed evidence remains off-target
    - therefore the remaining blocker on this case is structural candidate quality / packing
  - `6c49646a` still proves live aggregate activation, but not aggregate evidence quality
    - the route now activates as `aggregate`
    - this rerun still surfaced `graph: 0`, `temporal: 0` for the selected evidence path
    - so aggregate remains an unstable secondary target, not the main short-term blocker
- therefore `20.11` still should not jump to canary answer-only yet
- a later full post-join-fix canary retrieval-only rerun confirmed the carry-through repair at canary scope:
  - runtime sync still held (`28` compared files, `0` mismatch)
  - `5 / 5` completed in `318.259s`
  - `memory_context_present: 5 / 5`
  - non-fact routes remained `2 / 5`
  - `5d3d2817` now answers correctly in retrieval-only mode with the answer-bearing `turn 126` transcript row present in the final live memory block
  - `gpt4_7f6b06db` still demonstrates the unresolved structural-route problem more cleanly than before:
    - route is still `requested=temporal`, `applied=temporal`, `route_source=deterministic_route_hint`
    - channel counts are nonzero (`temporal: 14`, `graph: 8`)
    - but the final live memory block still does not contain the actual Muir Woods / Big Sur / Yosemite trip set
    - current targeted debug indicates those trip rows are largely absent from the fused pool, not merely dropped at the very last packing step
    - the retrieval-only harness answer here therefore must not be treated as retrieval proof; the remaining blocker is now best described as temporal candidate generation / extraction, with packing still downstream
- one bounded temporal micro-experiment was explicitly tried and rejected:
  - a strict relevance-shortlist-then-chronology variant was added for temporal selection
  - targeted live rerun on `gpt4_7f6b06db` showed it made the temporal `matched` rows worse by promoting generic “past three months” fitness/stamps rows
  - this patch was reverted immediately after the negative evidence
  - the current lesson is that the unresolved temporal problem is not solved by a naive relevance shortlist over the existing lexical signals
- a second bounded temporal micro-experiment was also explicitly tried and rejected:
  - a route-aware focused-search-query variant was added for temporal retrieval
  - targeted live rerun on `gpt4_7f6b06db` shifted transcript evidence toward Kyoto / attachment junk instead of surfacing the actual Muir Woods / Big Sur / Yosemite trip set
  - this patch was also reverted immediately after the negative evidence
  - the current lesson is that the unresolved temporal problem is not solved by naive query-shaping or temporal frame-token stripping over the current signals
- a third bounded temporal micro-experiment was also explicitly tried and rejected:
  - a route-level widening of continuity / transcript / semantic candidate-pool limits was added for temporal retrieval
  - targeted live rerun on `gpt4_7f6b06db` increased raw channel counts (`semantic: 24`, `keyword: 62`, `graph: 8`, `temporal: 14`) but still selected the same off-target turns (`matched: 25, 37, 35`; `transcript: 37, 38, 35`)
  - the real Muir Woods / Big Sur / Yosemite trip set still did not surface into the selected live evidence
  - this patch was also reverted immediately after the negative evidence
  - the current lesson is that the unresolved temporal problem is not solved by bounded search-pool widening over the current retrieval signals
- a richer `32/32/400` telemetry pass then made the remaining Tier-2 reliability picture much sharper:
  - `parse_status_counts` are now split as `{"empty_text": 1, "non_json": 5, "json_object": 3}`
  - successful write-bearing batch windows are now visible:
    - `97..128`
    - `193..224`
    - `230..277`
  - failing batch windows are also visible:
    - `1..32` as `empty_text`
    - `31..64`, `65..96`, `124..160`, `161..192`, `225..256` as `non_json`
  - several `non_json` payload previews begin with JSON-like objects rather than obvious prose
  - therefore the next justified move is raw payload failure-mode reading first, then parser/prompt/schema choice second
  - the trip-order miss is still not fully localized to a proven failing batch window, so the next step remains evidence-first rather than blind schema tightening
- two follow-up source fixes then materially changed the seed-time picture again:
  - compact Tier-2 output-shape rules were added so the extractor now prefers fewer, shorter items and compact JSON
  - temporal route selection was corrected to use `temporal_graph_rows` instead of the generic `graph_rows` pool
- after those fixes, a new targeted `gpt4_7f6b06db` rerun showed a real structural recovery:
  - `parse_status_counts` improved again to `{"non_json": 2, "json_object": 7}`
  - `batches_with_writes` rose to `7`
  - `total_writes` rose to `69`
  - backend population became materially denser (`entity=6`, `state=8`, `relation=1`, `inferred=3`)
  - the previously failing Yosemite batch `225..256` now parses successfully as `json_object`
  - that batch emits `temporal_events=7`
- this sharpens the live blocker again:
  - the relevant trip batch is now being extracted successfully
  - but the final captured memory still omits `Muir Woods`, `Big Sur`, `Monterey`, and `Yosemite`
  - therefore the dominant remaining problem is no longer seed-time Tier-2 reliability
  - it is now temporal-event surfacing / continuity selection: the extracted temporal event evidence is still not reaching the final memory block

## What 20.11 Does Not Yet Claim

- it does not yet claim retrieval recovery
- it does not yet claim canary answer-only improvement
- it does not yet claim that the profile / identity class is fully recovered just because one relevant row now survives into the live packed memory block
- it does not yet claim that the current RAG-style aggregate path can by itself carry the project above the older strongest benchmark band

## Recorded Forward Path After 20.11

- LongMemEval remains a calibration probe here, not the product objective
  - the real target is a SOTA Hermes memory kernel with coherent continuity, proactive state tracking, long-range usable recall, and meaningful token savings
  - `20.11` retrieval fixes should therefore be judged by whether they improve generic memory-kernel behavior, not by benchmark-specific trickery
- short-term retrieval recovery still looks bounded and realistic if these are improved:
  - temporal extraction / temporal-route evidence yield
  - fact / temporal packing quality
  - grounding carry-through on proven retrieval-correct rows
- the current `20.11` bottleneck reading is now more precise:
  - the live `5d3d2817` class is no longer a pure missing-evidence miss
  - the answer-bearing `turn 126` marketing-specialist transcript row now survives into the final live memory block after the join fix
  - the remaining problem is residual junk suppression / packing around that now-present evidence
  - this means the current cheap internal fix path and a future reranker / stronger-embedder path are aimed at the **same** bottleneck, just at different implementation layers
  - `20.11` has now exhausted the cheaper internal layer for the temporal-event case without closing it, so the residual temporal-event gap should move to `20.12` as a dedicated capability-phase
- a stronger asymmetric reranker / stronger embedder remains a valid next-phase architectural candidate
  - this is most plausible for profile / identity and other asymmetric question-to-statement retrieval classes
  - it should be recorded at the capability level first, but the current soft brand-level front-runner can also be named
  - it should no longer displace `20.11`; it is now the correct next-phase path for the residual temporal-event relevance problem
  - brand-level note:
    - the current leading external candidate family to evaluate is the Jina v5 text-embedding line via a TEI-style serving path
    - as of the public February 18, 2026 model-card claims, `jina-embeddings-v5-text-small` is presented as the strongest multilingual embedding option under 1B parameters on MTEB/MMTEB-style reporting
    - record this as a **tentative brand-level candidate**, not as a hard architectural rule and not as a final vendor lock
- medium-term aggregate recovery now has an explicit donor-first path recorded:
  1. improve Tier-2 typed entity extraction so graph state is not only generic entities / sparse relations
  2. if typed entities become reliable, add a bounded native aggregate query path on Kuzu for count/sum-style aggregate questions
- this is recorded as the strongest current medium-term path, not as a proven score guarantee

## Additional 20.11 Finding: Structured Temporal-Event Attempt

- a donor-first temporal-event path was added and validated locally:
  - Tier-2 can now emit bounded `temporal_events` with real `turn_number` anchors
  - reconciliation now persists those as `kind=\"temporal_event\"` continuity rows with temporal metadata
  - bounded tests now cover extractor normalization, empty-batch schema stability, and continuity persistence
- the source-side validation stayed green after this change:
  - source-targeted suite remained `48 passed`
  - real-world flow suite for the plugin namespace passed `25 passed`
- a targeted patched-runtime retrieval-only rerun for `gpt4_7f6b06db` still did **not** surface the real trip evidence in the final memory block:
  - `requested=temporal`, `applied=temporal`, `route_source=deterministic_route_hint`
  - the answer text came back correct, but the captured memory context still did **not** contain `Muir Woods`, `Big Sur`, or `Monterey`
  - this must therefore be treated as unsupported answer recovery, not as retrieval success
- the new parser instrumentation also clarified an important ambiguity:
  - the repeated `Tier2 extractor returned non-JSON payload` warnings do happen in the live seed path
  - but the logged failing batches were unrelated turn windows such as `turns=[89..96]`, `turns=[185..192]`, and `turns=[270..277]`
  - so the Tier-2 JSON truncation problem is real, but it is **not yet proven** to be the direct cause of the `gpt4_7f6b06db` miss
- a later benchmark-path telemetry pass tightened the Tier-2 reading further:
  - the default benchmark seed settings were confirmed to be unusually fat:
    - `benchmark_tier2_flush_turn_interval = 96`
    - `benchmark_tier2_transcript_limit = 192`
  - under that default `96/192` path, a targeted `gpt4_7f6b06db` rerun showed:
    - `batch_count: 3`
    - `parse_status_counts: {"non_json": 1, "json_object": 2}`
    - `batches_with_writes: 2`
    - `total_writes: 19`
    - graph population stayed very sparse (`entity=2`, `state=0`, `relation=0`, `inferred=1`)
    - route channels still read as `graph: 0`, `temporal: 6`
  - a bounded `32/32` rerun then showed that the seed batch size materially affects structural population:
    - `batch_count: 9`
    - `parse_status_counts: {"non_json": 6, "json_object": 3}`
    - `batches_with_writes: 3`
    - `total_writes: 32`
    - graph population improved to `entity=3`, `state=2`, `relation=1`, `inferred=1`
    - route channels improved to `graph: 4`, `temporal: 10`
  - but the final selected evidence for `gpt4_7f6b06db` still stayed off-target even after that structural improvement
  - a follow-up `32/32/900` truncation probe then clarified one more thing:
    - raising `benchmark_tier2_max_tokens` from `400` to `900` did **not** stabilize the extractor
    - the run degraded to `parse_status_counts: {"non_json": 7, "json_object": 2}`
    - and `total_writes` dropped from `32` to `14`
    - so the remaining Tier-2 reliability problem is **not** explained by token budget alone
  - therefore the correct reading is now:
    - benchmark-path Tier-2 seed sizing is a **real** contributor to structural population instability
    - but reducing the seed window alone is **not sufficient** to recover the true trip evidence
    - and simply raising Tier-2 `max_tokens` is explicit negative evidence, not the fix
    - the remaining blocker is best read as `Tier-2 seed reliability / structural population instability first`, then downstream temporal candidate quality
- net reading after this attempt:
  - the structural temporal-event path is now a validated code path
  - but it has **not yet produced positive live evidence** on the trip-order case
  - and the current `gpt4_7f6b06db` blocker now reads more precisely as seed-time Tier-2 reliability / structural population instability first, with temporal candidate quality and final packing downstream of that

## Handoff After 20.11

1. Treat `e9327a54` as the confirmed generic fact-path packing win from this round.
2. Treat `5d3d2817` as materially improved inside `20.11`; any later stronger reranker work should be framed as broader capability improvement, not as a claim that the join-fix did nothing.
3. Treat `gpt4_7f6b06db` as the explicit handoff case into `20.12`: temporal-event semantic retrieval / reranking for structured continuity.
4. Keep LongMemEval as a calibration probe only; `20.12` must be written as memory-kernel capability work, not benchmark-maxing.
5. Keep `6c49646a` as proof that aggregate activation is live again, but not yet as proof of aggregate evidence quality.
6. Treat the failed temporal shortlist, failed temporal query-variant, and failed bounded pool-widening experiments as explicit negative evidence; do not reopen temporal lexical shortlist/query-shaping/search-limit tweaks on the current signal set.
7. Carry the structural lessons forward without reopening `20.11`:
   - keep the `32/32/400` telemetry findings as evidence that cheap structural admission/persistence fixes helped materially
   - keep `32/32/900` as explicit negative evidence against “just raise Tier-2 max tokens”
   - keep the residual gap framed as relevance discrimination over already-structured temporal evidence
8. Treat the Jina v5 TEI path as a soft next-phase evaluation candidate only, not as a hard architectural rule.
9. Only after the `5d3d2817` junk-heavy fusion / packing issue and the `gpt4_7f6b06db` structural candidate-generation issue are tightened further should `20.11` consider another retrieval-only rerun.
10. Canary answer-only remains gated behind that retrieval-only recovery.
11. Record the next architectural escalation explicitly:
   - if the cheap internal fusion / packing path stops producing real canary wins, the next phase should test a stronger asymmetric reranker / stronger embedder path for the same junk-suppression bottleneck
   - that escalation should be treated as a new capability layer, not as justification to abandon the current `20.11` line prematurely
   - the leading named candidate to carry into that evaluation is currently the Jina v5 TEI path, but only as a test target rather than a precommitted final answer
12. Treat the current structured temporal-event work as an active but still unproven live path:
   - keep it donor-first
   - do not collapse the unsupported correct answer on `gpt4_7f6b06db` into a false success claim
   - and keep the Tier-2 non-JSON failures separate from the specific trip-order miss until a relevant failing batch is observed
13. Update the blocker classification after the compact-output + temporal-graph fix:
   - the `gpt4_7f6b06db` path is no longer best described as “Tier-2 extraction is failing”
   - the more precise live blocker is now:
     - successful temporal extraction exists for the relevant trip window
     - but extracted temporal-event evidence is still not being surfaced by continuity selection into the final memory block

## Additional 20.11 Finding: Query-Aware Temporal-Event Surfacing

- the first explicit temporal-event surfacing attempt proved that a dedicated cross-session temporal-event path is necessary but not sufficient:
  - without query-aware ordering, temporal mode still surfaced unrelated recent continuity rows
  - so the real requirement is explicit temporal-event retrieval **plus** temporal-first surfacing ahead of generic recent continuity
- a second structural pass then added:
  - dedicated `search_temporal_continuity(query=..., session_id=..., limit=...)` in the store
  - `kind="temporal_event"` restriction
  - cross-session retrieval
  - no hard positive-overlap survival requirement
  - query-aware ordering before observed-time ordering
  - temporal-mode selection that prioritizes explicit temporal-event rows ahead of generic recent continuity rows
- the resulting targeted patched-runtime rerun for `gpt4_7f6b06db` produced a real but partial win:
  - the final `recent` block no longer contained only unrelated holiday/prayer continuity junk
  - it now surfaced one relevant Yosemite temporal-event continuity row (`turn 230`) with `overlap_count=1`
  - this is the first live proof that extracted temporal-event continuity can reach the final selected memory block
- but the trip-order class is still not solved:
  - only one relevant temporal-event row surfaced
  - the final `matched` rows still stayed dominated by irrelevant continuity rows
  - the final transcript rows still stayed off-target
  - the captured memory still did **not** contain the full `Muir Woods -> Big Sur/Monterey -> Yosemite` evidence chain
- the blocker is therefore narrower again:
  - not route-hint
  - not basic Tier-2 parse reliability
  - not simple packing-only failure
  - but specifically:
    - dedicated temporal-event surfacing now works in principle
    - one relevant event can reach final memory
    - the remaining miss is multi-event temporal continuity ranking / selection breadth, because the full ordered trip set still does not surface together

## Additional 20.11 Finding: Trip-Window Stability Check

- a follow-up targeted rerun with `temporal_event_samples` exposed the next crucial truth:
  - the relevant trip window `225..256` did **not** stay in the recovered `json_object` state on this run
  - it regressed back to `json_parse_status = "non_json"`
  - `writes_performed = 0`
  - `temporal_events = 0`
  - `temporal_event_samples = []`
- the raw payload for that same failing batch is still strongly JSON-like:
  - preview starts with outdoor/Yosemite/JMT preference material
  - tail still contains trip-window event-like entries such as turn `233`
  - but the object remains truncated / unparsable, so none of that evidence is admitted
- this means the previous “ranking direction / selection breadth” reading was too strong as the primary blocker:
  - the explicit temporal-event surfacing path is real
  - but the full trip-order chain cannot surface if the relevant trip batch intermittently collapses back to `non_json`
- the current best blocker classification is therefore:
  - dedicated temporal-event surfacing works in principle
  - but the relevant trip-window Tier-2 output is still unstable
  - so the dominant blocker has shifted back to **trip-window-specific parser / output-shape stability**, ahead of any additional temporal ranking tweaks

## Additional 20.11 Finding: Reasoning-Fallback Admission Win, But Temporal Selection Still Misses

- a next structural pass added reasoning-field fallback to the Tier-2 extractor response reader:
  - `message.reasoning`
  - `message.reasoning_content`
  - `message.reasoning_details`
- this materially improved the benchmark-path admission truth for the targeted `gpt4_7f6b06db` run:
  - the `empty_text` class disappeared on the no-direct control run
  - `parse_status_counts` improved to fully admitted batches (`json_object` + `json_repaired` only)
  - the relevant trip batch `225..256` now wrote successfully with `temporal_events`
- an immediate control comparison also clarified that:
  - `--inject-direct-route-resolver` can still mask the real route truth by falling back to `source=default`
  - the correct control run for this case is the **no-direct** retrieval-only target
  - on that control run the question again routed as:
    - `requested=temporal`
    - `applied=temporal`
    - `source=deterministic_route_hint`
- however, the remaining temporal miss did **not** close after admission recovered:
  - the trip batch wrote temporal events (`turn 230..233+`)
  - yet those events still failed to dominate the final selected temporal rows
  - the selected temporal continuity block stayed polluted by older, weakly overlapping continuity rows
- a follow-up internal temporal-priority pass then confirmed another negative truth:
  - simply prioritizing temporal continuity ahead of generic recent rows is not enough
  - and simple lexical-hygiene cleanup of overlap stopwords is also not enough by itself
  - the final selected block still stayed off-target for the full trip-order chain
- the current best blocker classification is therefore narrower again:
  - not route-hint
  - not basic Tier-2 admission
  - not the direct route-injection path
  - but specifically:
    - **query-relevance discrimination inside temporal continuity selection remains too weak once multiple temporal_event rows exist**
    - the system can now admit and store the right trip-window events
    - but still cannot consistently choose them over unrelated temporal events when building the final memory block
