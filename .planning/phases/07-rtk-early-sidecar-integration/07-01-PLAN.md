---
phase: 07-rtk-early-sidecar-integration
plan: 01
type: execute
wave: 1
status: planned
created: 2026-04-10
depends_on:
  - 06.2-pragmatic-real-world-e2e-memory-proving
files_modified:
  - agent/rtk_sidecar.py
  - run_agent.py
  - tests/run_agent/test_rtk_sidecar_integration.py
  - .planning/phases/07-rtk-early-sidecar-integration/07-01-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - S1
must_haves:
  truths:
    - RTK acts as token/output discipline sidecar, not as a second memory system
    - integration should reuse existing tool-budget infrastructure instead of creating a parallel framework
    - measurable savings matter more than architecture theater
  artifacts:
    - agent/rtk_sidecar.py
    - tests/run_agent/test_rtk_sidecar_integration.py
    - .planning/phases/07-rtk-early-sidecar-integration/07-01-SUMMARY.md
  checks:
    - RTK budget policy is explicit in code
    - run_agent uses the RTK budget when enabled
    - tests prove savings and non-ownership
---

<tasks>
<task type="auto">
  <name>Add a bounded RTK sidecar module that maps config to output-budget policy</name>
  <files>agent/rtk_sidecar.py</files>
  <read_first>tools/budget_config.py, tools/tool_result_storage.py, .planning/phases/07-rtk-early-sidecar-integration/07-CONTEXT.md, .planning/phases/07-rtk-early-sidecar-integration/07-PLAN.md</read_first>
  <action>Create a small RTK sidecar module that turns config into a BudgetConfig and tracks simple savings statistics. Keep it sidecar-only: no memory ownership, no extra tools, no second control plane.</action>
  <verify>python -m py_compile agent/rtk_sidecar.py</verify>
  <done>RTK now has an explicit bounded role in code instead of only roadmap language.</done>
</task>

<task type="auto">
  <name>Wire RTK budget policy into run_agent tool-result handling and prove it with focused tests</name>
  <files>run_agent.py, tests/run_agent/test_rtk_sidecar_integration.py, .planning/phases/07-rtk-early-sidecar-integration/07-01-SUMMARY.md, .planning/STATE.md</files>
  <read_first>run_agent.py, tests/run_agent/test_run_agent.py, tests/run_agent/test_brainstack_integration_invariants.py, tools/tool_result_storage.py</read_first>
  <action>Use the RTK sidecar budget config at the existing maybe_persist_tool_result and enforce_turn_budget call sites, then add focused tests that show large tool output is reduced more aggressively and Brainstack still owns memory. Record the outcome and move state to the next verify step.</action>
  <verify>uv run --extra dev python -m pytest tests/run_agent/test_rtk_sidecar_integration.py tests/run_agent/test_brainstack_integration_invariants.py tests/run_agent/test_run_agent.py -q</verify>
  <done>RTK is now a real token-efficiency sidecar with proof, not a vague future note.</done>
</task>
</tasks>
