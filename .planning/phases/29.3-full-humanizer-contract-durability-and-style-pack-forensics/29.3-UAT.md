# phase 29.3 uat

## source verification

- `pytest tests/test_brainstack_phase29_style_contract.py tests/test_brainstack_phase24_correctness.py tests/test_install_into_hermes.py -q`
- result: pass

## live verification

Runtime target:
- `/home/lauratom/Asztal/ai/hermes-final`
- container: `hermes-bestie`

Verified facts:

1. canonical durable owner exists
   - active profile row exists for `preference:style_contract::principal_scope::platform:discord|user_id:935529517856985088|agent_identity:default|agent_workspace:hermes`

2. legacy corpus owner is retired
   - prior `style_contract` corpus document remains present only as inactive historical state

3. explicit recall works on the detailed lane
   - direct runtime prefetch for `Tudod a 29 szabályt?` resolved to:
     - `requested_mode = style_contract`
     - `applied_mode = style_contract`
   - the returned block contains the `Humanizer style contract`

4. ordinary-turn exclusion holds
   - direct runtime prefetch for `Beszélgessünk egy kicsit!` resolved to `fact`
   - the returned block does not contain the `Humanizer style contract`

5. ingest reliability hardening is active
   - installed config now uses `auxiliary.flush_memories.provider: main`
   - doctor passes on this check

## result

`6 / 6` pass for the intended phase contract.
