# Phase 20.3 Summary

## Outcome

This follow-on `20.3` execute slice closed the two named pending gaps that were left open after the first structural-routing pass:

- route-specific aggregate rendering / packing
- proof separation between retrieval correctness and LLM answer correctness

The structural routing work is therefore no longer only a retrieval-side change.
It now carries the matching presentation and proof-method pieces needed before the next bounded real-path benchmark proof.

## Source Changes

- updated [retrieval.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/retrieval.py)
  - aggregate route now renders a dedicated `## Brainstack Aggregate Evidence` section instead of reusing the generic fact/snippet treatment
  - aggregate evidence is presented as explicitly separate numbered recalled items
  - fact and temporal rendering remain unchanged
- updated [control_plane.py](/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/control_plane.py)
  - working-memory rendering now receives the applied route mode explicitly so rendering can follow routing intentionally
- updated [run_brainstack_longmemeval_subset.py](/home/lauratom/Asztal/ai/atado/Brainstack/scripts/run_brainstack_longmemeval_subset.py)
  - added explicit memory-context extraction for proof analysis
  - added a retrieval-support judge alongside the answer judge
  - report rows now carry:
    - `answer_correct`
    - `retrieval_correct`
    - `retrieval_judge`
    - `failure_layer`
  - this closes the proof-method gap where Brainstack and the answer model were previously blended into one failure bucket
- updated [test_brainstack_phase20_proof.py](/home/lauratom/Asztal/ai/atado/Brainstack/tests/test_brainstack_phase20_proof.py)
  - aggregate-route proof now asserts the dedicated aggregate evidence section
- added [test_brainstack_longmemeval_subset.py](/home/lauratom/Asztal/ai/atado/Brainstack/tests/test_brainstack_longmemeval_subset.py)
  - covers memory-context extraction
  - covers retrieval-vs-answer failure-layer classification

## Validation

- targeted structural routing + proof split + forensic visibility suite:
  - `tests/test_brainstack_phase20_proof.py`
  - `tests/test_brainstack_executive_retrieval.py`
  - `tests/test_brainstack_transcript_shelf.py`
  - `tests/test_brainstack_longmemeval_subset.py`
  - result: `27 passed`
- touched files passed `py_compile`

## Result

The two named post-`20.3` pending gaps are now closed:

- aggregate retrieval no longer loses its route identity at render time
- bounded proof output can now separate retrieval correctness from answer correctness

This does **not** yet claim that the real-path benchmark score has improved.
It does claim that the next bounded proof can be interpreted cleanly without conflating:

- wrong retrieval
- correct retrieval but wrong answer-model reasoning

Manual conversational UAT was intentionally skipped for this slice because it was not a meaningful operator-facing proof form for internal routing/render/proof-method behavior.

## Remaining Pending Gaps

- any future fail-open widening must remain explicitly bounded
  - slight bounded widening is acceptable
  - unbounded dump-more-rows fallback remains disallowed
- exact-value supersession remains separate technical debt
  - still important
  - still not the immediate next `20.3` cut

## Follow-up

- run the next bounded real-path proof on the updated `20.3` stack
- interpret the result through the new split:
  - retrieval correctness
  - answer correctness
- only then decide whether the next step is:
  - bounded structural refinement inside `20.3`
  - or a new phase beyond routing

## Bounded Real-Path Proof And Forensic Bundle

The bounded real-path proof work is now complete at the `20.3` level.

Generated reports:

- [brainstack-final-boss-20.3-legacy-comparability.json](/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-final-boss-20.3-legacy-comparability.json)
- [brainstack-final-boss-20.3-forensics-rerun.json](/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-final-boss-20.3-forensics-rerun.json)

Important note:

- [brainstack-final-boss-20.3-forensics.json](/home/lauratom/Asztal/ai/atado/Brainstack/reports/phase20/brainstack-final-boss-20.3-forensics.json) is now treated as obsolete for route analysis
- it was produced before the correct provider-state route capture was wired into the live runner

Comparability result on the current `20.3` stack:

- legacy answer-only runner: `3 / 15`

Current split-proof result on the current `20.3` stack:

- raw answer score: `5 / 15`
- raw accuracy: `0.3333`
- retrieval-correct count: `2 / 15`
- answer-correct count: `5 / 15`
- both retrieval-correct and answer-correct: `1 / 15`

Failure-layer split from the finished forensic rerun:

- `retrieval`: `9`
- `llm_answer`: `1`
- `answer_recovered_despite_retrieval_gap`: `4`
- `none`: `1`

Route-level proof facts:

- `applied_mode=fact` in `15 / 15`
- `route_source=fallback` in `14 / 15`
- the dominant fallback reason was live route-hint failure:
  - `401`
  - invalid / blocked / out-of-funds auxiliary route-hint key in the target runtime

Backend population sanity facts:

- `Chroma` is densely populated in this benchmark path
  - `section_count`: `209 .. 284`
  - nonzero in `15 / 15`
- `Kuzu` is sparse in this benchmark path
  - `entity_count`: `0 .. 2`
  - nonzero in only `3 / 15`
  - `relation_count`: `0 .. 1`
  - nonzero in only `1 / 15`

Interpretation:

- the `10 / 15 -> 5 / 15` gap is **not** just a measurement artifact
  - the legacy answer-only runner also regressed to `3 / 15` on the current `20.3` stack
- the new proof split remains necessary because the raw answer score still overstates Brainstack memory quality
- the dominant live problem is still retrieval, but the first-order live blockers are now more specific:
  - route-hint reliability is broken in the target runtime
  - graph/backend population is too sparse to support structural graph-heavy retrieval claims in this benchmark path

Additional proof note:

- judge reliability remains a real proof concern
- raw answer score must still be separated from:
  - retrieval-correct score
  - both-correct score
  - suspicious `judge=yes` passes

## Post-Proof Operational Decision

The bounded `20.3` forensic bundle is now complete.

This means `20.4` planning is no longer blocked on missing evidence.

What is now proven and should guide `20.4`:

- route-level proof metadata is available and working from provider state
- in-run backend population logging is available and necessary because the runner uses a per-question `TemporaryDirectory`
- the current live structural routing stack was not meaningfully exercised in proof because route hinting collapsed to fact fallback in `14 / 15`
- `Kuzu` sparsity and `Chroma` density are now measured facts for this benchmark path

What remains interpretation-sensitive and should stay explicit in the next phase:

- temporal / aggregate bonus-bypass remains a code-level architectural fact
- it is still **not** yet a benchmark-causal verdict on its own
- judge strictness / calibration remains a separate proof-method concern

Revised immediate conclusion:

- `20.3` improved architecture, rendering, and proof instrumentation
- `20.3` also surfaced the real live blockers more honestly than the earlier overlay-driven score lines
- the next step is now evidence-based `20.4` planning focused on:
  - live route-hint reliability
  - graph population / publication audit in the benchmark seeding path
  - proof and judge hardening where the new split already exposed weaknesses
