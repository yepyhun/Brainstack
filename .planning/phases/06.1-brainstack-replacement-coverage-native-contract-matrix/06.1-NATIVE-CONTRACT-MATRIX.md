# Phase 06.1 Native Contract Matrix

## Verdict
Brainstack now covers the host-used replacement-critical memory responsibilities that matter for native retirement. No blocking runtime gap remained after audit.

## Matrix
| Responsibility | Host path | Brainstack owner | Verdict | Evidence |
| --- | --- | --- | --- | --- |
| Stable profile block in system prompt | `run_agent.py::_build_system_prompt` -> `MemoryManager.build_system_prompt()` | `BrainstackMemoryProvider.system_prompt_block()` | matched | provider exposes `# Brainstack Profile`; existing displacement test proves builtin guidance stays out while Brainstack block remains |
| Per-turn prefetch / recall injection | `run_agent.py::run_conversation` -> `prefetch_all()` -> `build_memory_context_block()` | `BrainstackMemoryProvider.prefetch()` | matched | new replacement-contract test proves recalled Brainstack context is injected into API messages |
| Turn sync after conversation | `MemoryManager.sync_all()` | `BrainstackMemoryProvider.sync_turn()` | matched | existing plugin E2E plus new invariant test prove continuity rows are written into Brainstack storage |
| Pre-compress continuity snapshot | `run_agent.py::_compress_context` -> `MemoryManager.on_pre_compress()` | `BrainstackMemoryProvider.on_pre_compress()` | matched | new replacement-contract test proves `compression_snapshot` is persisted |
| Actual session-end summary / durable scan | `run_agent.py::shutdown_memory_provider` -> `MemoryManager.on_session_end()` | `BrainstackMemoryProvider.on_session_end()` | matched | new replacement-contract test proves `session_summary` and profile updates persist through shutdown |
| Built-in memory tool on live surface | `run_agent.py::_sync_builtin_memory_tool_surface` | none; explicitly blocked | intentionally_dropped | existing displacement tests prove `memory` tool disappears when builtin store is off and direct calls fail closed |
| Built-in flush path | `run_agent.py::flush_memories` | none; explicit non-ownership in displaced path | intentionally_dropped | host exits early when builtin live path inactive; no Brainstack dependency remains |
| Built-in background memory review | `run_agent.py` nudge/review path | none; explicit non-ownership | intentionally_dropped | existing displacement test proves review does not run when builtin path is displaced |
| Built-in user-profile prompt injection | `run_agent.py::_build_system_prompt` native block | superseded by Brainstack profile shelf | superseded | existing displacement test proves builtin user/profile prompt stays off while Brainstack profile stays on |
| Background queued prefetch optimization | `run_agent.py::queue_prefetch_all()` | no Brainstack implementation | intentionally_dropped | no functional dependency; synchronous prefetch before API call is the active ownership path |

## Notes
- `intentionally_dropped` here does not mean accidental loss. It means the displaced Brainstack path does not rely on that native behavior anymore.
- The one visible non-owned optimization is queued background prefetch. It is not a correctness gap because the live synchronous prefetch path is active and tested.
