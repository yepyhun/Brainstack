---
status: passed
phase: 29.2-practical-logistics-memory-capture-and-reminder-boundary-aud
source:
  - 29.1-01-SUMMARY.md
  - 29.2-01-SUMMARY.md
started: 2026-04-17T12:55:00Z
updated: 2026-04-17T14:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Durable identity row exists for age
expected: A durable principal-scoped `identity:age` row exists in live Brainstack storage after the 29.1 repair.
result: pass
evidence: Live `profile_items` contains active `identity:age::principal_scope::...` with content `19 years old`.

### 2. Direct age recall prefers durable identity truth
expected: A direct query like `Hány éves vagyok?` should surface the durable identity fact through the durable identity lane, not mainly through continuity/transcript fallback.
result: pass
evidence: Live deployed packet now classifies `Hány éves vagyok?` as `profile_slot_targets = ('identity:age',)`, enters compact profile-first policy, and returns `identity:age = 19 years old` as the first durable profile row.

### 3. Core communication contract projects into the deployed prompt
expected: The deployed prompt should contain the core user-specific communication contract needed for runtime behavior: Bestie naming, Humanizer instruction, Hungarian response rule, pronoun-capitalization rule, and newline preference.
result: pass
evidence: Deployed prompt block contains Bestie naming, Humanizer style, Hungarian response instruction, pronoun-capitalization, and separate-thought line guidance.

### 4. Stable provider/place truth becomes durable graph truth
expected: `Móni` provider/place truth should be queryable from the durable graph shelf, with a clean current address and category value.
result: pass
evidence: Live graph search returns current `Móni address = Kassák Lajos 87 44es kapucsengő 4em` and `category = talpmasszázs`.

### 5. Transient same-day todos stay out of durable shelves
expected: `Fodrász / Bank` should not be promoted into durable profile or current graph truth by default.
result: pass
evidence: Live `profile_items` and current `graph_states` contain no `Fodrász` / `Bank` durable rows.

### 6. Chron remains the reminder owner
expected: Reminder execution state should remain in native Chron jobs, not be duplicated as Brainstack scheduler truth.
result: pass
evidence: Live `/opt/data/cron/jobs.json` contains the talpmasszázs reminder jobs with `state = scheduled` and `deliver = origin`.

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0
