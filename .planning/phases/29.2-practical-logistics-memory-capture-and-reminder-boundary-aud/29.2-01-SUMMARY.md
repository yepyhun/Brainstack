# phase 29.2 summary

## result

Phase `29.2` is execute-complete at gate.

The final boundary verdict is:

- stable provider/place truth belongs in durable Brainstack storage
- transient same-day todos do not
- Chron remains the scheduler and reminder-delivery owner

## what was proven

### live shelf audit before repair

- Móni / Kassák / talpmasszázs facts were present in transcript and continuity
- they were not present in profile or graph shelves
- `Fodrász / Bank` also surfaced only through transcript/continuity
- Chron already held the real reminder jobs in native scheduler state

### final repair shape

- no new shelf
- no reminder shadow database
- no todo promotion rule-engine
- existing typed-entity / graph shelf was reused
- a bounded logistics helper was added for explicit provider/place declarations only
- a versioned compatibility migration repaired old transcript history into typed-entity graph truth

### deployed-path proof after repair

- rebuilt the `hermes-final` runtime image
- force-recreated the running `hermes-bestie` container
- verified the running container contains:
  - `logistics_contract.py`
  - `stable_logistics_typed_entities_v2`
  - transcript user-segment stripping
- triggered a real prompt build in the running container so Brainstack opened the live store
- verified the live DB now contains:
  - `stable_logistics_typed_entities_v1`
  - `stable_logistics_typed_entities_v2`
  - current graph truth for `Móni`
  - current `address = Kassák Lajos 87 44es kapucsengő 4em`
  - current `category = talpmasszázs`
- verified the earlier malformed prefixed address is now historical, not current
- verified `Fodrász / Bank` still does not appear in:
  - `profile_items`
  - current graph states
- verified Chron still owns the actual reminder jobs in `/opt/data/cron/jobs.json`

## ruled out

- storing all logistics text in profile rows
- duplicating Chron scheduler state into Brainstack
- promoting same-day errands into durable memory by default
- fixing the problem by transcript luck alone

## final architectural reading

The correct 29.2 boundary is:

- Brainstack owns durable provider/place truth that has later recall value
- Chron owns reminder execution state
- transient practical todos remain non-durable unless a later phase explicitly promotes a narrower class with proof

## recommended next step

- run a combined verify pass for `29.1` and `29.2` on the deployed Discord path

## recommended model level

- `high`
