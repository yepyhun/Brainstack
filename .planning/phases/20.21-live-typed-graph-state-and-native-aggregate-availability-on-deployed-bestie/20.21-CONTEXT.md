# Phase 20.21 Context

## Why this phase exists

`20.19` proved that deployed Bestie can activate donor-aligned graph and corpus backends.

`20.20` then proved a smaller bridge truth:
- aggregate routing can preserve corpus support in source/runtime logic
- but live aggregate proofs still do **not** show selected `native_aggregate` rows
- focused live debug still reports:
  - active `semantic` / `graph` channels
  - `graph candidate_count = 0`
  - no selected `graph_rows`
  - no selected `corpus_rows`
  - no selected `native_aggregate`

At the same time:
- `aggregate_total` can pass in live Bestie
- but it still passes through transcript arithmetic, not graph-native evidence
- the stronger road-trip total proof still fails live (`510 miles` instead of `605`)

So the remaining blocker is no longer “packet bridge forgot graph/corpus.”
The more honest reading is:
- live typed graph-state / native aggregate evidence is not being produced or surfaced in the deployed path strongly enough to compete at all

## Inputs from prior phases

- `20.15` built the bounded native Kuzu aggregate path
- `20.19` enabled live `kuzu` / `chromadb` runtime dependencies
- `20.20` proved that aggregate-route bridge hardening helps at source/runtime level, but does **not** by itself make live native aggregate evidence appear

## Current product truth

- deployed Bestie now runs with active graph/corpus backends
- isolated post-`20.20` live baseline is `4 / 5`
- only `temporal_order` still fails in the main isolated live eval
- but focused aggregate live debug still shows the more important aggregate truth:
  - `aggregate_total` is still not a clean native-aggregate proof
  - `native_total_distance` still fails live
  - active channels are present without meaningful graph/native candidate production

## What this phase is not

- not another keyword/temporal degraded-stack micro-tuning pass
- not broader aggregate planner expansion
- not a new scorer/reranker phase
- not a packaging/deploy redesign phase
- not a benchmark-shaped tuning loop

## Honest target

Produce truthful evidence for one of these two outcomes:

1. live typed graph-state / native aggregate availability can be restored enough that deployed Bestie actually surfaces graph-native aggregate evidence on a known aggregate case

or

2. the blocker is upstream of selection, and the exact missing production seam is localized honestly for the next phase
