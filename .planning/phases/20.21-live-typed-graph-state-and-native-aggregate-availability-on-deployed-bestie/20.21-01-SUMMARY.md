# Phase 20.21 Summary

Status: execution-complete at gate

## What changed

- Live reset/finalize now passes the warm cached agent into the background Brainstack finalize task before cache eviction.
- This preserves the provider instance that accumulated `_pending_tier2_turns`, so `on_session_end()` can actually run the session-end Tier-2 flush and graph persistence path.
- Added a gateway regression test to lock this reset/finalize ordering in place.

## Files touched

- Bestie runtime:
  - `gateway/run.py`
- Bestie tests:
  - `tests/gateway/test_session_boundary_hooks.py`

## Validation

- Gateway reset/finalize regression slice:
  - `18 passed`
- Focused live availability proof:
  - `reports/phase20/brainstack-20.21-live-graph-availability-debug.json`
  - direct evidence after `/reset`:
    - `graph_entities = 4`
    - `graph_states = 12`
    - typed graph state includes `road_trip`, `owner_subject = User`, and `distance_miles` values
  - live recall after reset:
    - correct answer: `605 miles`
    - `graph` channel active with `candidate_count = 9`
- Post-fix isolated deployed-live reset eval:
  - `reports/phase20/brainstack-bestie-live-reset-eval-isolated.json`
  - result: `5 / 5`

## Honest reading

- The missing seam was real and local:
  - `/reset` scheduled the finalize task
  - then immediately evicted the cached agent
  - so Brainstack finalize often fell back to a cold temp agent and skipped the warm provider state
- After the fix, live typed graph-state is materially present on deployed Bestie.
- This is not answer-only fake green:
  - graph state is physically present in the live DB after reset
  - graph retrieval is active and returns candidates on the recall turn
  - the isolated deployed-live eval recovered to `5 / 5`

## Carry-forward note

- The current proof establishes live graph-native availability and graph candidate presence.
- It does not prove that every future aggregate truth will always surface as an explicit selected `native_aggregate` row rather than other graph-native evidence.
- That is now a secondary hardening topic, not the main blocker.

## Next step

- Checkpoint the current `20.17–20.21` live runtime gains.
- If continuing immediately in GSD, prefer a broader deployed-live product validation / quality phase over another aggregate micro-tuning phase.

## Recommended effort

- `high`
