---
phase: 06-native-memory-displacement-completion
plan: 01
type: execute
wave: 1
depends_on:
  - 01-brainstack-composite-provider-foundation
  - 05-orchestrator-working-memory-control-plane
files_modified:
  - .planning/phases/06-native-memory-displacement-completion/06-01-PLAN.md
  - .planning/phases/06-native-memory-displacement-completion/06-LEGACY-PLAN-NOTES.md
  - run_agent.py
  - tests/run_agent/test_brainstack_native_memory_displacement.py
  - .planning/phases/06-native-memory-displacement-completion/06-01-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - R5
  - R6
  - R7
  - A1
  - A2
  - A3
  - A4
must_haves:
  truths:
    - Built-in prompt memory and built-in user profile behavior stay fully off when the built-in store is not active
    - The built-in memory tool disappears from the live tool surface when there is no active built-in memory store
    - The host patch remains small and explicit instead of creating a second hidden orchestration layer in Hermes
    - Brainstack remains the single host-facing composite provider for the replacement flow
  artifacts:
    - .planning/phases/06-native-memory-displacement-completion/06-01-PLAN.md
    - run_agent.py
    - tests/run_agent/test_brainstack_native_memory_displacement.py
    - .planning/phases/06-native-memory-displacement-completion/06-01-SUMMARY.md
  key_links:
    - 06-01-PLAN.md aligns with ROADMAP.md Phase 6 promises and Phase 1 ownership constraints
    - STATE.md advances to the correct next official step once Phase 6 execution is complete
---

<objective>
Implement the minimum Hermes host patch set that fully displaces the native live memory path for the Brainstack flow.

Purpose: Make Brainstack the active replacement path without leaving the built-in memory tool, prompt guidance, or memory-review loops half-alive in the runtime.
Output: A GSD-compliant Phase 6 plan, a minimal run_agent displacement patch, focused host-level tests, and a verification-ready project state.
</objective>

<execution_context>
@/home/lauratom/.codex/get-shit-done/workflows/execute-plan.md
@/home/lauratom/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PROGRAM-EXECUTION.md
@.planning/phases/06-native-memory-displacement-completion/06-CONTEXT.md
@.planning/phases/06-native-memory-displacement-completion/06-LEGACY-PLAN-NOTES.md
@.planning/phases/01-brainstack-composite-provider-foundation/01-IMPLEMENTATION-CONTRACT.md
@run_agent.py
@agent/memory_manager.py
@agent/builtin_memory_provider.py
@plugins/memory/brainstack/__init__.py
@tests/agent/test_memory_plugin_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Normalize Phase 6 into a GSD-executable plan and freeze the displacement boundary</name>
  <files>.planning/phases/06-native-memory-displacement-completion/06-01-PLAN.md, .planning/phases/06-native-memory-displacement-completion/06-LEGACY-PLAN-NOTES.md</files>
  <read_first>.planning/phases/06-native-memory-displacement-completion/06-CONTEXT.md, .planning/phases/06-native-memory-displacement-completion/06-PLAN.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROGRAM-EXECUTION.md, .planning/phases/01-brainstack-composite-provider-foundation/01-IMPLEMENTATION-CONTRACT.md</read_first>
  <action>Convert the freeform Phase 6 note into the official GSD `{phase}-{plan}-PLAN.md` format and preserve the original note as legacy context. Lock in the boundary: this phase removes built-in live-path dependence, but does not turn Hermes into a broad hard fork.</action>
  <verify>node /home/lauratom/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/06-native-memory-displacement-completion/06-01-PLAN.md</verify>
  <acceptance_criteria>
    - `.planning/phases/06-native-memory-displacement-completion/06-01-PLAN.md` exists
    - `06-01-PLAN.md` contains `phase: 06-native-memory-displacement-completion`
    - `06-01-PLAN.md` contains `<objective>`
    - `06-01-PLAN.md` contains at least one `<task type="auto">`
    - the plan explicitly states that Brainstack is the one host-facing replacement path for this flow
  </acceptance_criteria>
  <done>The phase is parseable by GSD tools and no longer blocked on freeform planning format.</done>
</task>

<task type="auto">
  <name>Implement explicit native live-path displacement in run_agent with the smallest safe host patch</name>
  <files>run_agent.py</files>
  <read_first>.planning/phases/01-brainstack-composite-provider-foundation/01-IMPLEMENTATION-CONTRACT.md, run_agent.py, agent/builtin_memory_provider.py, agent/memory_manager.py, plugins/memory/brainstack/__init__.py</read_first>
  <action>Add a single explicit host-side rule that removes the built-in `memory` tool from the live tool surface when the built-in memory store is not active, keeps built-in prompt memory and user-profile prompt injection off, and makes any stray built-in memory invocation fail closed instead of silently half-working. Keep the patch narrow and upstream-friendly.</action>
  <verify>rg -n "builtin memory|live path|memory tool|disabled for this session|_sync_builtin_memory_tool_surface" run_agent.py</verify>
  <acceptance_criteria>
    - the built-in `memory` tool is removed from the live tool surface when no built-in memory store exists
    - built-in memory guidance is not injected into the system prompt when the live path is displaced
    - built-in memory flush and memory-review triggers do not run when the live path is displaced
    - a direct call into the built-in `memory` branch fails closed with a clear disabled response instead of mutating state
  </acceptance_criteria>
  <done>Hermes no longer depends on the native built-in memory live path for the Brainstack flow.</done>
</task>

<task type="auto">
  <name>Add focused host-level displacement tests and move project state to verification-ready</name>
  <files>tests/run_agent/test_brainstack_native_memory_displacement.py, .planning/STATE.md</files>
  <read_first>run_agent.py, tests/agent/test_memory_plugin_e2e.py, tests/run_agent/test_strict_api_validation.py, .planning/STATE.md</read_first>
  <action>Add targeted tests around AIAgent initialization, tool-surface cleanup, system-prompt behavior, disabled flush/invoke behavior, and background-review suppression for the displaced path. Then update the project state so the next official command points to Phase 6 verification instead of leaving execution ambiguous.</action>
  <verify>uv run --extra dev python -m pytest tests/run_agent/test_brainstack_native_memory_displacement.py -q && rg -n "/gsd-verify-work 6|Phase 6|06-01-PLAN.md" .planning/STATE.md</verify>
  <acceptance_criteria>
    - `tests/run_agent/test_brainstack_native_memory_displacement.py` exists
    - tests cover tool-surface removal, prompt behavior, disabled flush/invoke behavior, and review suppression
    - `.planning/STATE.md` references `/gsd-verify-work 6` when Phase 6 execution is complete
  </acceptance_criteria>
  <done>Phase 6 is test-backed and the project state routes to the correct next official gate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node /home/lauratom/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/06-native-memory-displacement-completion/06-01-PLAN.md`
- [ ] `cd /home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-latest && node /home/lauratom/.codex/get-shit-done/bin/gsd-tools.cjs phase-plan-index 6`
- [ ] `uv run --extra dev python -m pytest tests/run_agent/test_brainstack_native_memory_displacement.py -q`
</verification>

<success_criteria>

- All tasks completed
- The plan is GSD-parseable and no longer blocks Phase 6 execute semantics
- Hermes built-in memory live-path dependence is removed for the Brainstack flow
- Phase 6 is ready for `/gsd-verify-work 6`
</success_criteria>

<output>
After completion, create `.planning/phases/06-native-memory-displacement-completion/06-01-SUMMARY.md`
</output>
