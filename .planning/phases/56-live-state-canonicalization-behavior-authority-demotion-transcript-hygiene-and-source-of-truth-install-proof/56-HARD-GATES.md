# Phase 56 Hard Gates

## hard gate 1: source-of-truth discipline

- all code edits land in:
  - `/home/lauratom/Asztal/ai/atado/Brainstack-phase50`
- if `finafina` requires a code fix that cannot be reproduced by install/copy from source of truth, the phase fails

## hard gate 2: canonical deployed explicit truth

- installed `USER.md` is canonical, not degraded bundled prose
- installed `USER_PROFILE_INDEX.json` aligns with the canonical native truth
- the installed runtime no longer depends on degraded legacy entries plus LLM repair to behave correctly

## hard gate 3: no active behavior authority for explicit native rule packs

- `behavior_contracts` contains no active row representing the native explicit rule pack
- `compiled_behavior_policies` contains no active row representing the native explicit rule pack
- explicit native packs may exist only as:
  - native Hermes truth
  - bounded Brainstack mirror/archive

## hard gate 4: transcript hygiene

- internal runtime/status strings such as interrupt/waiting/status text do not persist into transcript memory
- transcript recall cannot surface those internal runtime/status strings as conversational evidence

## hard gate 5: fresh install proof

- a fresh Hermes checkout is configured successfully by the wizard/install path from `Brainstack-phase50`
- the installed files match the intended corrected source surfaces

## hard gate 6: final installed-runtime Discord proof

- installed runtime / provider-path verification is executed on the installed runtime in `finafina`
- real Discord UI verification is also executed on the installed runtime in `finafina`
- no hand-patched-only state is relied upon
- the observed runtime is free of the inspector-blocking defects named in this phase

## hard gate 7: no false closure

- the phase cannot be called complete merely because the system feels “cleaner”
- the phase cannot be called complete while active authority residue still exists
- the phase cannot be called complete from fresh-state-only proof
