# Phase 29.4 Summary

## outcome

Phase `29.4` is closed.

The oracle regression runner is trustworthy again for the bounded retrieval-only gate that must run before any new live `27`-rule recall diagnosis.

## what changed

- repaired `scripts/run_brainstack_longmemeval_subset.py` so pure `--retrieval-only --oracle-seed` mode no longer hard-requires benchmark API credentials
- removed the old retrieval-only harness dependence on a separate remote Tier-2 override path
- rebuilt the temporary benchmark home from the real runtime config instead of a partial benchmark-only config
- copied the real runtime auth artifacts into the temporary oracle home so `main` provider resolution behaves like deployed Hermes
- added an explicit backend-dependency gate so the runner fails honestly if the benchmark interpreter is missing configured Brainstack backends
- aligned the benchmark interpreter with the deployed runtime by installing the same `kuzu` and `chromadb` dependencies

## evidence

- targeted runner suite passed
  - `28 passed`
- broader regression bundle passed
  - `53 passed`
- bounded oracle retrieval-only rerun completed
  - processed: `60 / 60`
  - `memory_context_present`: `60 / 60`
  - `backend_population_nonzero`: `60 / 60`
  - `zero_backend_cases`: `0`
  - `empty_text_batches`: `0`
  - route pairs:
    - `fact/fact`: `25`
    - `aggregate/aggregate`: `20`
    - `temporal/temporal`: `15`
- runtime sync stayed clean during the oracle run
  - compared files: `31`
  - mismatches: `0`

## verdict

- the old oracle harness really was broken
  - it was coupling retrieval-only proof mode to remote API wiring and incomplete runtime parity
- the current bounded oracle gate does not show a broad regression from the recent `29.x` Brainstack work
- therefore the next problem is not “the whole memory kernel regressed”
- the next problem is the live detailed style-contract path itself

## comparison to prior oracle baseline

Earlier recorded `70`-item oracle spillover:

- `memory_context_present`: `70 / 70`
- `backend_population_nonzero`: `48 / 70`
- `zero_backend_cases`: `22`
- `empty_text_batches`: `19`

Current bounded `60`-item oracle gate:

- `memory_context_present`: `60 / 60`
- `backend_population_nonzero`: `60 / 60`
- `zero_backend_cases`: `0`
- `empty_text_batches`: `0`

This is not a publishable benchmark claim.

It is the correctness gate we needed:

- the runner now behaves like a real regression harness again
- there is no oracle-level “vészhelyzet” that would justify reopening the whole recent product path blindly

## next exact handoff

Open the next phase on one narrow question:

- why does the live canonical `preference:style_contract` row still contain only a shortened ruleset instead of the full taught contract?

Recommended next step:

- execute `29.5`

Recommended model level:

- `xhigh`
