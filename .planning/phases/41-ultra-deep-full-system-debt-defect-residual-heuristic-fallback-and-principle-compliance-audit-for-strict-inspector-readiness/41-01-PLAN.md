# Phase 41 Plan

## goal

Produce a complete, unsparing, inspector-grade inventory of all still-open defects, technical debt, residual heuristics, fallback seams, proof gaps, deploy drifts, and principle-compliance risks across the Brainstack source repo and the deployed Hermes/Bestie runtime.

This phase is an audit phase, not a stealth implementation phase.

## scope

### in

- source-level defect and debt audit across:
  - routing
  - retrieval
  - write paths
  - memory authority
  - graph and legacy seams
  - fallback and fail-open behavior
  - proof and test gaps
  - deploy/runtime/auth/config drift
- explicit review against:
  - `/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-latest/.planning/IMMUTABLE-PRINCIPLES.md`
- incremental preservation of findings in durable audit artifacts
- distinction between:
  - fixed
  - still open
  - residual but bounded
  - deploy-only drift

### out

- broad implementation work beyond small emergency fixes discovered during audit
- release preparation
- benchmark polishing
- vague product-readiness claims without evidence

## canonical principle reference

- `/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-latest/.planning/IMMUTABLE-PRINCIPLES.md`

## workstreams

### 1. routing and retrieval residuals

- inspect control-plane and executive retrieval residual heuristics
- inspect route-failure semantics and economic failure behavior
- inspect remaining query-shape dependence

### 2. memory authority and write surfaces

- inspect remaining durable write seams
- inspect fallback authority reconstruction risks
- inspect store convergence, repair, and lineage edge cases

### 3. graph, legacy, and multimodal seams

- inspect typed graph ingress limits
- inspect legacy graph compatibility surfaces
- inspect whether current graph/value path still stays multimodal-safe

### 4. runtime and deploy drift

- inspect Docker/runtime-only warnings, auth drift, config drift, and environment assumptions
- split product bug vs deploy drift vs upstream/runtime friction

### 5. proof and test debt

- inspect missing gates
- inspect false-green risk
- inspect areas where live behavior is not yet covered by deterministic proof

### 6. principle compliance verdict

- enumerate any direct or residual conflict with:
  - donor-first
  - fail-closed upstream compatibility
  - zero heuristic sprawl
  - multimodal-first architecture
  - truth-first / no good enough

## first proven findings batch

- Discord slash command sync failure from oversized `/skill` payload in the deployed gateway
- OpenRouter `402` payment failures affecting Brainstack route-resolution and auxiliary memory paths
- CA bundle path drift warning in Hermes auth/runtime TLS handling

## deliverables

- `41-AUDIT-LOG.md`
- file-level findings grouped by severity
- explicit “principle non-compliance” subsection
- explicit “runtime drift vs source bug” subsection
- explicit “strict inspector likely attack surface” subsection

## validation ladder

1. source and runtime evidence captured for each finding
2. no finding written without a concrete file or runtime proof reference
3. findings split by severity and ownership
4. findings preserved in batches so context compaction cannot erase detail

## recommended model level

- `xhigh`
