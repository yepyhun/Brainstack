# 20.20 Summary

Status:
- `20.20` execution-complete, but **not** gate-complete.
- real bounded win: aggregate-route bridge hardening landed in source and mirror runtime
- honest blocker: deployed live path still does **not** prove `native_aggregate` surfacing

Touched code:
- Brainstack source:
  - `/home/lauratom/Asztal/ai/atado/Brainstack/brainstack/executive_retrieval.py`
  - `/home/lauratom/Asztal/ai/atado/Brainstack/tests/test_brainstack_phase20_proof.py`
- Bestie runtime mirror:
  - `/home/lauratom/Asztal/ai/memory-repo-bakeoff/hermes-agent-bestie-latest/plugins/memory/brainstack/executive_retrieval.py`

What changed:
- aggregate-route support no longer ignores `graph_rows` / `corpus_rows` when deciding whether aggregate retrieval has enough support
- aggregate route now preserves corpus-backed rows instead of hard-dropping them with `corpus_rows: []`
- aggregate route explicitly accepts corpus-only support in source proofs

Validation:
- source proof slice:
  - `test_brainstack_phase20_proof.py -k 'stream_b_proves_graph_delta_above_l1_baseline or stream_c_proves_corpus_delta_above_non_corpus_baseline or phase20_20_aggregate_route_preserves_corpus_rows or phase20_20_aggregate_route_accepts_corpus_only_support or native_aggregate'`
  - result: `4 passed`
- Bestie minimum regression slice:
  - `20 passed`
- focused live debug:
  - `reports/phase20/brainstack-20.20-live-debug.json`
- post-patch isolated live re-baseline:
  - `reports/phase20/brainstack-bestie-live-reset-eval-isolated.json`
  - result: `4 / 5`

Key truths:
- source-level bridge hardening is real:
  - aggregate route preserves corpus rows in tests
  - aggregate route can stay aggregate with corpus-only support in tests
- deployed live enablement is still only partially bridged:
  - `aggregate_total` passes
  - but focused live debug still shows:
    - `graph_rows = 0`
    - `corpus_rows = 0`
    - `has_native_aggregate = false`
  - therefore `aggregate_total` is **still not** a clean live native-aggregate proof
- the stronger road-trip total proof still fails live:
  - `native_total_distance` answered `510 miles`
  - expected `605 miles`
  - focused live debug again showed no selected graph/corpus/native rows
- isolated live re-baseline improved to `4 / 5`, with only `temporal_order` still failing

Verdict:
- `20.20` was worth doing.
- It closed a real source/runtime bridge gap in aggregate selection logic.
- But it also proved that the remaining live blocker is **not** “aggregate route forgets corpus rows.”
- The honest remaining blocker is upstream live availability / production of graph-native aggregate evidence and its carry-through into the final packet.

Recommended next step:
- add + plan `20.21`
- scope:
  - live typed graph-state / native aggregate availability on deployed Bestie
  - why enabled graph runtime still yields `graph candidate_count = 0` on live aggregate scenarios
  - why active semantic/corpus channels still do not materially enter final selected rows in the focused live aggregate path

Recommended effort:
- `xhigh`
