# Phase 20.20 Plan

## Goal

Bridge the now-enabled graph/corpus stack into the final live packet on deployed Bestie, prove native aggregate surfacing on the live path, and re-baseline enabled-stack live stability without reopening degraded-stack tuning.

## Scope

### In

- enabled-stack packet bridge diagnosis for live Bestie
- bounded surfacing changes so graph/corpus/native rows can actually matter in the final packet
- one explicit proof that `aggregate_total` can surface through native aggregate evidence rather than only through transcript arithmetic
- one enabled-stack isolated live stability re-baseline
- explicit reading of remaining misses after the bridge work

### Out

- broader aggregate capability expansion
- new graph/corpus backend types
- new scorer/reranker branches
- checkout/deploy packaging overhaul
- benchmark-specific heuristics

## Execution Steps

1. Workstream A: enabled-stack packet bridge diagnosis.
   - Start from `20.19` truth:
     - channels are active
     - final packets are still transcript/continuity-heavy
   - Localize why enabled graph/corpus evidence is not becoming decisive in the selected packet.
   - Use bounded packet proofs on the known cases:
     - `aggregate_total`
     - `temporal_order`
     - `gift_context`
   - Separate:
     - channel activation
     - candidate availability
     - native aggregate presence inside `matched` versus the separate `graph_rows` / `corpus_rows` shelves
     - selected-row dominance
     - answer synthesis

2. Workstream B: bounded enabled-stack bridge hardening.
   - Adjust only the existing selection/surfacing path so live graph/corpus/native evidence can win when it is clearly on-target.
   - Preferred bounded directions:
     - selection priority rebalance for enabled graph/corpus rows
     - native aggregate row visibility in aggregate mode
     - packet composition changes that preserve donor-first structure
   - Treat the existing aggregate-mode native prepend as the starting point; do **not** add a second parallel bypass path unless the current one is proven insufficient.
   - Do not widen into new aggregate planner classes here.

3. Workstream C: native aggregate surfacing proof.
   - Produce one explicit proof that `aggregate_total` is no longer merely a transcript-arithmetic success.
   - The proof should show a surfaced packet or selected-row path where native aggregate evidence is materially present.
   - If that cannot be achieved honestly, say so explicitly.

4. Workstream D: enabled-stack isolated live stability re-baseline.
   - Re-run the isolated deployed-live reset eval after the bridge changes.
   - Compare against the current enabled-stack baseline:
     - `3 / 5`
   - Read specifically:
     - whether `aggregate_total` remains green for the right reason
     - whether `temporal_order` improves
     - whether `gift_context` remains stable instead of flipping back

5. Workstream E: honest next-phase decision.
   - If the enabled-stack bridge materially improves live truth, say so directly.
   - If stability remains weak, classify that residual honestly.
   - If the main remaining blocker is no longer packet bridge, identify the new dominant class instead of stretching this phase.

## Implementation Rules

- do not reopen degraded-stack keyword/temporal tuning
- do not expand the aggregate planner surface beyond what is needed to prove native aggregate surfacing
- do not fake native aggregate usage from a correct final number alone
- do not blur channel activation and selected-packet dominance into one claim
- do not treat `graph_rows = 0` alone as proof that native graph evidence failed to surface; check `matched` for `native_aggregate` rows explicitly
- keep checkout/deploy config truth explicit; do not round partial runtime truth up to “fully deployed”

## Output Shape Required From Phase 20.20

- packet-level before/after reading for the enabled stack
- one explicit native aggregate surfacing proof
- one enabled-stack isolated live re-baseline
- one honest classification of the remaining residual class

## Exit Gate

- there is direct evidence that enabled graph/corpus/native rows can materially surface into the final live packet
- `aggregate_total` is not only “correct by arithmetic luck”, but bridged through surfaced enabled-stack evidence
- one enabled-stack isolated live re-baseline exists after the bridge work
- the next-thread recommendation is based on enabled-stack product truth, not fallback ambiguity

## Recommended Effort

- `xhigh`
