---
status: partial
phase: 10.1-brainstack-integration-kit-upstream-update-workflow
source:
  - 10.1-01-SUMMARY.md
started: 2026-04-10T19:39:00Z
updated: 2026-04-10T19:54:00Z
---

## Current Test

number: 4
name: [testing paused — 1 item outstanding]
expected: |
  The core install/update path is in place, but runtime-mode coverage is still incomplete because local non-Docker operation has not been added yet.
awaiting: user response

## Tests

### 1. Dry-run install sanity
expected: A dry-run install command says the fresh Hermes checkout is compatible, Brainstack is not installed yet but can be installed cleanly, and no hidden half-wired blocker exists.
result: issue
reported: "Jó, de ne csak Dockeres indulást kezeljen; legyen nem-Dockeres futtatási mód is."
severity: major

### 2. Real install activates Brainstack
expected: After real install, the fresh Bestie runtime selects Brainstack as the active memory provider and Hermes builtin memory plus builtin user profile are both off.
result: pass

### 3. Docker rebuild survives runtime-state files
expected: Rebuilding the Dockerized Bestie runtime should not fail because of container-owned config or runtime files; the build context should avoid baking mutable runtime state into the image.
result: pass

### 4. Docker Bestie runtime is healthy
expected: After rebuild, the Dockerized Bestie runtime should start Hermes Gateway mode, the desktop launcher should still point at that runtime, and container health should become healthy instead of false-unhealthy.
result: pass

### 5. Upstream refresh path is reusable
expected: There should now be a simple install/update/doctor flow in the Brainstack repo that can be reused after future upstream Hermes updates without manual file copying.
result: issue
reported: "Ezt még be kell fejezni és utána ezt feltölteni a repoba."
severity: major

## Summary

total: 5
passed: 3
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "The integration kit supports both Docker and non-Docker Hermes runtime modes through the same install and doctor workflow."
  status: failed
  reason: "User reported: Jó, de ne csak Dockeres indulást kezeljen; legyen nem-Dockeres futtatási mód is."
  severity: major
  test: 1
  root_cause: "The first implementation completed the shared install core, but runtime-specific validation and launcher assumptions were finished only for the Docker Bestie path."
  artifacts:
    - path: "/home/lauratom/Asztal/ai/atado/Brainstack/scripts/install_into_hermes.py"
      issue: "No runtime mode selection yet."
    - path: "/home/lauratom/Asztal/ai/atado/Brainstack/scripts/brainstack_doctor.py"
      issue: "Doctor checks are Docker-oriented instead of runtime-mode aware."
  missing:
    - "Add runtime mode support such as docker vs local while keeping one shared installer."
    - "Make doctor checks conditional by runtime mode rather than implicitly Docker-first."
    - "Document both runtime flows in the Brainstack README."
  debug_session: ""

- truth: "The reusable update path is complete enough to publish only after both Docker and local runtime modes are covered by the same install/update/doctor kit."
  status: failed
  reason: "User reported: Ezt még be kell fejezni és utána ezt feltölteni a repoba."
  severity: major
  test: 5
  root_cause: "Phase 10.1 shipped a reusable core, but publication-quality completeness is blocked by missing local runtime mode support."
  artifacts:
    - path: "/home/lauratom/Asztal/ai/atado/Brainstack/scripts/install_into_hermes.py"
      issue: "Installer does not expose runtime mode selection yet."
    - path: "/home/lauratom/Asztal/ai/atado/Brainstack/scripts/brainstack_doctor.py"
      issue: "Doctor still assumes Docker-oriented runtime checks as the main path."
    - path: "/home/lauratom/Asztal/ai/atado/Brainstack/README.md"
      issue: "README documents install/update flow but not the future two-mode runtime contract."
  missing:
    - "Finish runtime mode support for local non-Docker Hermes execution."
    - "Teach doctor to validate local runtime mode separately from Docker mode."
    - "Update README to document both runtime modes and when to use each."
    - "Only after that, push the finished integration kit to the Brainstack repo."
  debug_session: ""
