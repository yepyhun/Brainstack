# Phase 29.10 Implementation Contract

## Required invariants

1. Every ordinary-turn answer has an inspectable active behavior-policy identity.
2. Active compiled behavior policy and raw archival contract can be compared without manual DB inspection.
3. Behavior-policy correction/supersession is explicit and does not rely only on blind re-teaching.
4. If runtime/install state is stale or incompatible, doctor/parity checks fail closed.

## Minimum acceptable outcomes

- a bounded trace payload exists
- a bounded inspection surface exists
- a bounded correction path exists
- doctor/runtime parity checks cover behavior-policy activation

## Prohibited shortcuts

- no hidden shadow owner
- no runtime regex rewriter as the main policy mechanism
- no “probably active” claims without traceable revision/hash state

## Execute notes

- provider trace must cover both ordinary-turn behavior-policy surfaces:
  - `system_prompt_block`
  - `prefetch`
- docker doctor dependency probes must prefer the real gateway interpreter path before generic `python3`
  - current verified path: `/opt/hermes/.venv/bin/python3`
## recommended model level

`high`
