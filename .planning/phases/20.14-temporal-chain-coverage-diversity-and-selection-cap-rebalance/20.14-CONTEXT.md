# Phase 20.14 Context

## Why This Phase Exists

- `20.13` closed the upstream availability question at gate.
- The dominant upstream blocker was benchmark-path session mixing plus undersized per-flush generation windows.
- A bounded benchmark-path fix now exists:
  - `--benchmark-tier2-flush-mode session_boundary`
  - exact session-turn transcript override per flush
- Oracle-only `session_boundary + 900` proof restored the desired trip chain at the generation / persistence layer.
- The residual `gpt4_7f6b06db` miss therefore moved downstream:
  - not scorer existence
  - not route-hint
  - not pure temporal-event absence

## Current Truth Entering 20.14

- `20.13` is execution-complete at gate.
- The current extractor can generate:
  - Muir Woods
  - Big Sur / Monterey
  - Yosemite
  under session-aligned bounded proof.
- The final selected temporal block still does not preserve enough distinct trip coverage.
- The strongest current explanation is no longer generic “breadth”.
- The strongest current explanation is:
  - temporal selection cap compression
  - missing diversity constraints in final temporal selection
  - possible dedupe side-effects that still need explicit verification

## What This Phase Is

- a selection-layer follow-up phase over already-restored temporal events
- a bounded temporal chain coverage phase
- a donor-first phase focused on:
  - cap arithmetic
  - diversity in selected temporal-event rows
  - dedupe guard verification
- a memory-kernel continuity phase, not a benchmark solver phase

## What This Phase Is Not

- not another scorer debate
- not another upstream generation / persistence phase
- not a return to lexical query shaping
- not route-hint work
- not a full retrieval-stack rewrite
- not a product-path mandate for `session_boundary`

## Entry Conditions

- runtime-sync verification remains mandatory
- the bounded temporal semantic rerank layer remains in place
- the env-gated external scorer hook remains preserved
- `session_boundary` remains available as a bounded benchmark-path proof tool
- session-clean DB audit and explicit dedupe guard remain active interpretation rules

## Target End State

- the project can say, with evidence, whether the residual trip-chain miss is caused primarily by:
  - too-small temporal caps
  - missing diversity in the selected temporal-event set
  - dedupe collapsing distinct trip evidence
  - or a named interaction between those three
- one bounded generic selection-layer fix exists if the dominant cause is clear enough
- the targeted temporal proof shows better trip-chain coverage in the final selected block without reopening scorer or generation confusion
